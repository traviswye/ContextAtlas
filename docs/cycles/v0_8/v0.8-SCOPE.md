# v0.8-SCOPE.md — V1.0 ship-gate closure + cohort onboarding completion

**Status:** Scope-doc drafted at v0.8 cycle pre-planning per
Direction D' refined lock + 4 substep cluster framework
(Adjudications 1-2 + LOCKs A-H at v0.8 cycle pre-planning
design adjudication surface). Cycle pre-planning inheritance
from v0.7 cycle close per
[`v0_8-HANDOFF.md`](v0_8-HANDOFF.md) bridge document substrate
(commit `49ca82c`).

**Cycle character.** v0.8 is **launch-bearing-closure** —
substantively the last big cycle before v1.0 public launch.
V0.8 closes V1.0 ship-gate criterion #1 statistical via Stream
B matrix-completion + closes v1.0 launch-narrative cohort-
onboarding-pipeline surface via /prime-atlas Skill substrate +
absorbs TERTIARY user-trust pre-launch gates carried-since-v0.4
+ absorbs bounded mechanical substrate hygiene + closes
ROADMAP.md substrate-currency gap at cycle close.

**Cycle target.** V1.0 launch follows v0.8 (next cycle IS v1.0,
not v0.9). V1.0 ship-gate state post-v0.8: **3-of-3 MET + 1
deferred-by-design** (criterion #3 external dogfood deferred to
v1.0 launch trigger — v1.0 IS the cohort exposure event).
Substantively cleaner launch narrative vs current post-v0.7
"2-of-3 MET + 2 carried-forward."

---

## §1 Cycle thesis

V0.8 ships ContextAtlas v1.0-launch-ready substrate via 4
substep cluster scope per Direction D' refined lock (Travis +
dev + advisor convergent triangulation at v0.8 cycle pre-
planning):

- **Cluster 1 — Stream B matrix-completion** (benchmarks-repo
  parallel cadence). Full 24-cell × n=5 × 2 conditions = 240
  trials at single fixed atlas-substrate; F9 tag-AND-control
  discipline applied. Closes V1.0 ship-gate criterion #1
  statistical-meaningful-wins (UPGRADED from △ PARTIAL via v0.6
  8-cell subset to ✓ MET via v0.8 full matrix-completion).
- **Cluster 2 — TERTIARY A1+A2+A3 absorption** (main-repo
  sequential). Carried-since-v0.4 user-trust pre-launch gates
  absorbed pre-v1.0 launch surface; cohort-trust substantively
  cheaper at v0.8 than post-launch remediation.
- **Cluster 3 — /prime-atlas Skill substrate** (main-repo
  sequential). Closes v1.0 launch-narrative cohort-onboarding-
  pipeline surface end-to-end: atlas scaffolding via
  /index-atlas + ADR foundation via /generate-adrs + per-
  session priming via /prime-atlas. Substantively the third
  Skill substrate completing the v1.0 launch cohort onboarding
  flow.
- **Cluster 4 — Bounded mechanical absorption** (main-repo
  sequential). FO-15 + FO-16 + SDK upgrade ^0.27.0 → ^0.32.0;
  substrate hygiene at substrate maturity.
- Plus ROADMAP.md substrate-currency gap absorption at v0.8
  cycle close substep (revision history reflects v0.5/v0.6/v0.7
  /v0.8 cycle progression + v0.8+ block refinement per v0.8
  cycle thesis lock).

**Substrate-generation thesis preserved** via post-launch
infrastructure inheritance per v0.6 Step 9.0 Q9.0.11 lock
(recruitment + observability + ADR-20 consent contract +
feedback template + onboarding documentation ship-ready for
v1.0 launch trigger consumption). v0.8 cycle does NOT generate
cohort substrate; inherits v0.6/v0.7-shipped infrastructure for
v1.0 launch-trigger consumption.

**V1.0 ship-gate criteria post-v0.8 launch (3-of-3 MET + 1
deferred-by-design per launch-bearing closure framing):**

- ✓ #1 quality-axis methodology parenthetical CLOSED at v0.5
  (preserved through v0.6 + v0.7 + v0.8)
- ✓ #1 statistically-meaningful-wins ABSORBED at v0.8 Cluster 1
  via Stream B matrix-completion full 24-cell × n=5 × 2 (was △
  PARTIAL via v0.6 8-cell subset; UPGRADED to ✓ MET)
- ✓ #2 onboarding pipeline shipped at v0.7 SECONDARY 3-repo
  install/setup empirical verification (preserved through v0.8;
  /prime-atlas Skill at Cluster 3 substantively completes
  cohort-onboarding-pipeline surface end-to-end)
- △ #3 external dogfood DEFERRED-BY-DESIGN — v1.0 launch
  trigger IS the cohort exposure execution event; recruitment
  infrastructure shipped at v0.6 ready for v1.0 launch-trigger
  consumption

**Cycle target.** Launch-bearing-closure timeline preceding v0.9
administrative cycle (repo hygiene + doc reorganization + npm
deployment) preceding v1.0 public launch trigger. v0.9 cycle
scope content NOT in v0.8 cycle adjudication surface; locked at
v0.9 cycle pre-planning per established cycle pre-planning
pattern. Internal working target ~3-4 weeks per LOCK H wall-clock
envelope. Substantively bounded per Travis Lock 2 no-wall-clock-
ceiling discipline preserved.

**Critical cycle-discipline constraint.** NOTHING from v0.8
pushes to v0.9. v0.9 substantively bears administrative-
completion weight only. Any v0.8 candidate substantively cannot
complete in v0.8 carries forward to `research/v1.1-candidates.md`
substrate per LOCK 2 backlog file disposition (NOT to v0.9).
Load-bearing-to-launch items in v0.8 cycle scope substantively
MUST complete in v0.8 — non-negotiable per launch-readiness
discipline. Discipline rationale: keeping v0.9 framing
private at v0.8 cycle thinking prevents substrate-erosion risk
where v0.8 cycle work substantively softens because "v0.9 will
catch it." V0.8 cycle thesis substantively bears full launch-
readiness weight.

---

## §2 Cycle scope

Per Adjudication locks A-H at v0.8 cycle pre-planning design
adjudication surface. 4 substep clusters per Option γ ordering
(main-repo sequential + benchmarks-repo parallel) substantively
matching v0.6 Stream A/B/C concurrent pattern precedent.

### Cluster 1 — Stream B matrix-completion (benchmarks-repo parallel cadence)

**Scope per LOCK C + LOCK D:**

- **Full 24-cell × n=5 × 2 conditions = 240 trials** at single
  fixed atlas-substrate. Substantive methodology argument per
  LOCK C: F1 confound observation + Option β cross-cycle
  stitching substantively repeats F9 methodological gap; Option
  α full 24-cell at single fixed atlas-substrate produces clean
  within-cycle data canonically.
- **F9 tag-AND-control discipline applied per LOCK D.1:**
  per-trial run manifest substrate (~50-100 LOC at benchmarks-
  repo `scripts/`) captures:
  - `contextatlas.version_label`: v0.8.X (cycle tag)
  - `atlas.substrate.version`: v0.8.0 atlas schema + extraction
    substrate version (NOT just package version)
  - `atlas.substrate.commit_sha`: contextatlas commit SHA at
    atlas extraction time
  - `atlas.target.commit_sha`: target repo pinned-SHA
  - `extraction.substrate.fingerprint`: hash of (extraction
    prompt + model + effort + adapter version)
  - `methodology.cycle`: v0.8 (vs v0.6 / v0.5)
  - `methodology.amendments`: list of F-amendments applied at
    v0.8 (F3 + F5 + F9)
- **F3 cell-selection empirical pre-screen** (~100-150 LOC at
  benchmarks-repo `scripts/v0.8-cell-screen.mjs`): n=2 dry-run
  trials per candidate cell before n=5 commitment. Substantive
  methodology improvement; cell-selection methodology lacked
  empirical differentiation pre-screen at v0.5 + v0.6.
- **F5 variance-control auto-stretch refinement** (substrate-
  improvement at benchmarks-repo orchestration scripts): extend
  hono h1 auto-stretch pattern to other high-variance cells
  with budget-controlled limit. v0.6 observation: 4 of 5 v0.5
  anchors INCREASED variance in v0.6 substrate; F5 refinement
  bounds variance impact on N=240 sample.
- **RUBRIC.md amendment per LOCK D.2** (~30-50 LOC; at v0.8
  cycle close cycle-doc updates substep): formalize F9 tag-AND-
  control as canonical methodology requirement at canonical
  methodology surface; cohort users + future cycles inherit;
  prevents F1-style confound recurrence.

**Scope boundaries:**

- Single fixed atlas-substrate throughout cycle (v0.8.0 atlas;
  no mid-cycle substrate-shift). F1 confound observation
  explicitly demonstrated 28-100% effect attenuation under
  cross-cycle atlas-substrate-shift; v0.8 cycle methodology
  controls for this via single-substrate discipline.
- Cross-cycle comparison to v0.5/v0.6 historical data NOT in
  v0.8 scope — F1 causal mechanism investigation deferred per
  Adjudication 4 to v1.0+ post-launch analytical work.
- F2/F4/F6/F7/F8 observation-only per LOCK D.3 (no specific
  amendment work):
  - F2 anchor-cell attenuation direction-uniform — observation
  - F4 2-axis retry policy — already absorbed at v0.6 Step
    5.3.a
  - F6 cross-order agreement attenuated on hallucination —
    observation
  - F7 cost-projection accuracy improved — observation
  - F8 F6 reproduction at higher incidence — observation

**Closure criteria:** V1.0 ship-gate criterion #1 statistical
closes when Stream B matrix-completion produces clean within-
cycle data per F9 discipline; matrix-completion outcome per-axis
distinguishability framing per ADR-19 §4 paired-t cross-cell
rollup at N=240 substrate (>3x larger than v0.5 N=27 + v0.6 8-
cell subset).

### Cluster 2 — TERTIARY A1+A2+A3 absorption (main-repo sequential)

**Scope per LOCK F (ordering: A1 first → A2+A3 paired):**

- **A1: `classifyError` catch-all conflates JSON-parse
  exceptions with API errors** (`research/v0.5-candidates.md`
  #1; carried v0.5/v0.6/v0.7). Fix: dedicated `ParseError`
  class + classifier branch distinguishing parse failures
  (deterministic; recheck LLM output shape) from API failures
  (transient; retry with backoff).
  - Engineering estimate: ~30-50 LOC `src/extraction/`
    classification refactor + ~5-10 new tests; ~0.5 cycle day.
  - Cohort-impact: User-trust at v1.0 launch — cohort users
    encountering extraction failures get substantively
    distinguishable error messages enabling self-diagnosis.

- **A2: `extractDocstringsForFile` non-idempotent at symbol
  level** (`research/v0.5-candidates.md` #2; carried
  v0.5/v0.6/v0.7). Paired with A3. Fix: symbol-id-keyed
  idempotency at extraction time (not write time); re-extracted
  file's claim set replaces prior claim set keyed by
  `(file_path, symbol_id)`.
  - Engineering estimate: ~40-80 LOC `src/extraction/`
    idempotency model + ~10-15 new tests; ~1 cycle day.
  - Cohort-impact: User-trust + substrate-maintainability —
    cohort users at v1.0 launch otherwise see atlas grow
    unboundedly across re-extractions.

- **A3: `pipeline.ts` Stage 5 deletion handling defeats Stream
  C idempotency** (`research/v0.5-candidates.md` #3; paired
  with A2). Fix: claim-source-aware deletion sweep + file-
  deletion idempotency at pipeline Stage 5.
  - Engineering estimate: ~50-100 LOC `src/extraction/
    pipeline.ts` Stage 5 refactor + ~10-15 new tests; ~1 cycle
    day.
  - Cohort-impact: User-trust + substrate-correctness — cohort
    users at v1.0 launch get clean atlas state across file-
    deletion scenarios.

**Cluster wall-clock:** ~3-5 cycle days bounded.

**Closure criteria:** All 3 substrate-gap fixes shipped with
test coverage; npm test all-PASS baseline preserved per CLAUDE.md
src-changes-require-full-test discipline (1527 tests / 85 files
baseline at v0.7 ship gate); cohort-trust user-facing gates
closed pre-v1.0 launch.

### Cluster 3 — /prime-atlas Skill substrate (main-repo sequential)

**Scope per LOCKs B.1 through B.5:**

- **Skill substrate naming: `/prime-atlas`** per LOCK B.1
  (substance-over-timing semantics; namespace conflict
  avoidance; matches /index-atlas + /generate-adrs action-verb
  + atlas-or-substrate-object pattern; Travis substantive lean
  /start walked back at dev's engineering judgment surface).
- **MCP introspection: Option β tool-call probe** per LOCK B.2.
  Skill loads atlas.json (via Read tool) + picks sentinel
  symbol (first symbol in atlas.json `symbols[]`) + probes
  `get_symbol_context` call. Probe outcomes:
  - Success → MCP connected; session ready; surface tools-
    introduction prompt
  - Tool-not-found → MCP not connected; surface remediation
    (instruct user to verify .mcp.json + restart Claude Code)
  - Atlas.json missing → instruct user to run
    `contextatlas init` + `/index-atlas` first
- **`.mcp.json` template: Option α checks-only** per LOCK B.3.
  /prime-atlas verifies .mcp.json present + contextatlas server
  entry present; if missing → surface `contextatlas init`
  remediation pointer. Existing `src/init/mcp-registration.ts`
  substrate (v0.6 Step 4.4 Q4.0.10 lock; idempotent upsert with
  registered/preserved/merged result statuses) inherits without
  modification.
- **Tools-introduction prompt: Option β substantive when-to-use
  guidance** per LOCK B.4 (~80-150 LOC). Matches /index-atlas +
  /generate-adrs SKILL.md substrate scale. Content:
  - 3 MCP tools enumeration: `get_symbol_context` +
    `find_by_intent` + `impact_of_change`
  - When-to-use guidance per tool (architectural prompt
    patterns vs primitive-Grep patterns; bundle-output framing
    via compact format)
  - Edge cases (unknown symbol → ERR not_found; FTS5 query
    patterns for find_by_intent)
  - Cohort UX hints (atlas-version awareness; SHA-diff
    incremental refresh; Phase 4 cost framing)
- **Per-session entry point semantics: Option α manual invoke**
  per LOCK B.5. Cohort discipline-dependent pattern; auto-prime
  patterns are v0.8+ Claude-Code-feature-dependency candidates
  (CC2/CC3 framing in `research/v1.1-candidates.md` per LOCK 2
  rename at v0.8 cycle close). Per-
  session entry point: cohort users at v1.0 launch document
  "Run `/prime-atlas` at start of each Claude Code session in
  your ContextAtlas repo."

**Engineering shape:**

- `.claude/skills/prime-atlas/SKILL.md` (~200-300 LOC; matches
  /index-atlas 346 LOC + /generate-adrs 259 LOC substrate
  scale)
- YAML frontmatter: `name: prime-atlas` + `description: ...` +
  `model: claude-opus-4-7` + `effort: xhigh` (matches v0.7
  SKILL.md frontmatter pattern per Step 2.3.c.0 substrate-
  evolution lock)
- "When to use this skill" section (cohort invocation contexts;
  per-session entry point pattern)
- "What this skill does" section (numbered procedural steps:
  load atlas.json → pick sentinel symbol → probe get_symbol_
  context → surface tools-introduction prompt)
- Tools-introduction prompt section (~80-150 LOC per LOCK B.4)
- Failure modes section (atlas missing; MCP not connected;
  symbols empty)
- Tool usage section (Read tool against .contextatlas/atlas.
  json; MCP tools)

**Empirical validation:** /prime-atlas Skill substantively
absorbs `research/v1.1-candidates.md` item #5 partial (per LOCK
2 rename at v0.8 cycle close from `research/v0.8-candidates.md`)
— contextatlas-on-itself Skill cohort entry path empirical
validation; /prime-atlas Skill empirical run at contextatlas-on-
itself substantively validates Skill cohort entry path
substrate.

**Cluster wall-clock:** ~3-5 cycle days bounded.

**Closure criteria:** /prime-atlas Skill substrate shipped at
`.claude/skills/prime-atlas/SKILL.md`; tests cover tool-call
probe + atlas.json presence checks + .mcp.json verification;
empirical validation at contextatlas-on-itself dogfood
substrate; CLAUDE.md cohort UX section + README.md cohort entry
paths section reference /prime-atlas alongside /index-atlas +
/generate-adrs at v0.8 cycle close cycle-doc updates.

### Cluster 4 — Bounded mechanical absorption (main-repo sequential)

**Scope per LOCK E (ordering: SDK first → FO-15 + FO-16
paired):**

- **SDK upgrade ^0.27.0 → ^0.32.0** (first; closes type-cast
  workaround at β-1):
  - Mechanical edit: package.json line bump
  - Regression sweep: npm install + npm test full baseline
    (1527 / 85 all PASS preserved per CLAUDE.md discipline)
  - Type-API migrations if needed (SDK 0.32+ types `thinking`
    parameter natively; clear inline cast workaround at
    `src/generation/generators/anthropic-api-direct.ts`)
  - Engineering estimate: ~1 LOC package.json + ~5-10 LOC cast
    removal + regression sweep; ~30 min-4 hours wall-clock per
    migration scope.

- **FO-15: validate-atlas-side `contextatlas_version`
  invariant** (paired with FO-16 at validate-atlas surface):
  - Mechanical enforcement: `generator.contextatlas_version`
    field present AND parses as semver AND matches installed
    package version OR is in allowlist of supported versions
  - Engineering estimate: ~30-50 LOC `src/extraction/cli-
    validate-atlas.ts` + ~5-8 new tests; ~1 cycle day.

- **FO-16: timestamp format invariant** (paired with FO-15 at
  validate-atlas surface; sub-adjudication LOCK E:
  validate-atlas-side mechanical enforcement vs SKILL.md
  instruction alternative — Option α mechanical enforcement
  locked):
  - Mechanical enforcement: `generated_at` field parses as ISO
    8601 + within reasonable bound of file mtime OR current
    time at validate invocation
  - Engineering estimate: ~20-40 LOC + ~3-5 new tests; ~1 cycle
    day.

**Cluster wall-clock:** ~3-5 cycle days bounded.

**Closure criteria:** SDK upgrade clean (npm test all-PASS at
^0.32.0); FO-15 + FO-16 validate-atlas-side mechanical
enforcement shipped with test coverage; canonical schema
invariants strengthened pre-v1.0 launch.

### Cycle close cycle-doc updates substeps (ROADMAP.md + backlog file rename + v0_9-HANDOFF.md substrate generation)

**Scope per LOCK G + LOCK 2 (combined cycle close cycle-doc
updates substeps; absorbed alongside per v0.5/v0.6/v0.7 cycle
close substep cluster precedent):**

#### ROADMAP.md absorption per LOCK G:

- Revision history entries for:
  - v0.5 (2026-05-04 ship; LLM-judge methodology +
    quality-axis blind-grading)
  - v0.6 (2026-05-09 ship; early-access pipeline-mechanics +
    8-cell matrix subset + cohort infrastructure)
  - v0.7 (2026-05-12 ship; launch-bearing cycle to v1.0
    substrate complete)
  - v0.8 (date TBD; V1.0 ship-gate closure cycle)
- v0.8+ block refinement per v0.8 cycle thesis lock:
  - Stream B matrix-completion ABSORBED (8-cell subset → full
    matrix at v0.8)
  - F1 causal investigation DEFERRED to v1.0+
  - Cohort exposure execution DEFERRED-BY-DESIGN to v1.0
    launch trigger
  - Task-shaped queries DEFERRED to v1.1+ feature-unlock cycle
  - Cohort UX refinement DEFERRED to v1.0+ post-launch
- v0.9 block (NEW per LOCK 1 v0.9 administrative cycle soft-
  reference): administrative cycle scope (repo hygiene + doc
  reorganization + npm deployment) preceding v1.0 launch
  trigger; v0.9 cycle scope content locked at v0.9 cycle pre-
  planning per established pattern; v0.9 bears NO v0.8 carry-
  forward work per discipline constraint
- v1.0 ship-gate criteria evolution preserved (3-of-3 MET + 1
  deferred-by-design post-v0.8 substantively cleaner launch
  narrative vs current post-v0.7 2-of-3 MET + 2 carried-
  forward)
- v1.0 block: launch trigger framing (v1.0 IS the cohort
  exposure event; recruitment infrastructure shipped ready)
- v1.1+ block: task-shaped bundle queries (why_does_this_fail
  + onboard_to_feature + audit_change) per ROADMAP Key
  efficiency unlocks table; references `research/v1.1-
  candidates.md` substrate per LOCK 2 backlog file rename

#### Backlog file rename per LOCK 2 (`research/v0.8-candidates.md` → `research/v1.1-candidates.md`; Option α):

- Rename file at canonical path
- Update header framing:
  - File purpose revised: working substrate for v1.1+ scope-
    doc drafting consumption (NOT v0.8 cycle pre-planning
    substrate any longer)
  - Honest scope-locking disclaimer carry-forward per existing
    framing pattern
  - Cross-reference updated to `v0_9-HANDOFF.md` substrate
    (generated at v0.8 cycle close per v0_7-HANDOFF.md +
    v0_8-HANDOFF.md precedent)
  - CRITICAL DISCIPLINE NOTE: file substantively contains
    items DEFERRED BECAUSE non-load-bearing-to-launch — NOT
    items "pushed from v0.8 to v1.1" (subtle but substantively
    meaningful framing per LOCK 1 discipline constraint
    preservation)
- Annotate absorbed-at-v0.8 items inline per per-candidate
  substantive detail preservation:
  - Stream B matrix-completion absorbed candidates annotated
    "ABSORBED at v0.8 Cluster 1 [Stream B matrix-completion]
    + F3 + F5 + F9 amendments" (F3 cell-selection pre-screen
    + F5 variance-control + F9 tag-AND-control discipline
    items per v0.7-SCOPE.md §3 5 methodology amendment items)
  - TERTIARY A1+A2+A3 absorbed candidates annotated "ABSORBED
    at v0.8 Cluster 2 [TERTIARY A1+A2+A3]"
  - /prime-atlas Skill partial-absorbed candidate #5 (Skill
    cohort entry path empirical validation) annotated
    "PARTIAL-ABSORBED at v0.8 Cluster 3 [/prime-atlas Skill
    empirical validation at contextatlas-on-itself dogfood]"
  - Mechanical absorption candidates #8 (FO-15) + #9 (FO-16)
    + #10 (SDK upgrade) annotated "ABSORBED at v0.8 Cluster 4
    [Mechanical absorption]"
- Carry-forward candidates preserved at v1.1+ substrate with
  framing refinement per LOCK 1 discipline constraint:
  - Cohort UX refinement candidates 11-14 (deferred BECAUSE
    post-launch refinement work; load-bearing-to-launch
    discipline preserved)
  - Substrate-evolution depth candidates 1-7 except #5
    partial-absorbed (deferred BECAUSE non-load-bearing-to-
    launch; preserved at v1.1+ per discipline constraint)
  - Test substrate candidate 15 — flaky LSP-interaction tests
    inventory (deferred BECAUSE non-load-bearing-to-launch;
    test-substrate-maintainability post-launch work)
  - Cross-cycle inheritance items CC1-CC3 (Claude Code
    feature dependencies; substantively non-actionable at
    ContextAtlas surface)
  - F1 atlas-substrate-version confound causal mechanism
    investigation (deferred to v1.0+ post-launch analytical
    work per Adjudication 4)
- v0.8 cycle-emergent observations added to v1.1+ candidate
  substrate IF substantively warranted at v0.8 cycle close
  (per substantive observation emergence during cycle
  execution at substep cluster maturity surface)

#### Cross-references updated at canonical surfaces per LOCK 2:

- `v0_8-HANDOFF.md` §2 substrate-inheritance framing
  (research/v0.8-candidates.md → research/v1.1-candidates.md
  reference; substantively updates v0_8-HANDOFF.md at v0.8
  cycle close alongside; preserves audit trail at v0.7→v0.8
  handoff substrate)
- `CLAUDE.md` "v0.8+ candidates" Current Version block
  (v0.8+ → v1.1+ pointer update; v0.7 outcome block
  preserved as historical record per v0.6/v0.5 pattern)
- `ROADMAP.md` v1.1+ block reference per LOCK G ROADMAP
  absorption

#### v0_9-HANDOFF.md substrate generation per v0_7-HANDOFF.md + v0_8-HANDOFF.md precedent:

- §1-§4 framework structure mirrors v0_8-HANDOFF.md (~150-200
  LOC bounded per LOCK 6 v0.7 precedent inheritance):
  - §1 Cycle-completion narrative (v0.8 ship commit SHA +
    v0.8.0 annotated tag + cross-repo back-reference SHA +
    cycle close commit SHA + package bump observation +
    CLAUDE.md Current Version block update)
  - §2 Substrate-inheritance framing (research/v1.1-
    candidates.md reference + Class-15/16/17/etc. observations
    + v0.8-SCOPE.md verification outcome + V1.0 ship-gate
    criteria evolution)
  - §3 v0.9 cycle pre-planning surface (administrative cycle
    scope per LOCK 1 discipline constraints; repo hygiene +
    doc reorganization + npm deployment; v0.9 cycle scope
    content locked at v0.9 cycle pre-planning)
  - §4 v0.8-SCOPE.md absorbed-item annotations (Cluster 1 +
    Cluster 2 + Cluster 3 + Cluster 4 + ROADMAP cycle close
    all ✓ ABSORBED at v0.8 cycle close)

**Engineering estimate (combined cycle close cycle-doc updates
substeps):**

- ROADMAP.md update: ~30-60 min
- `research/v0.8-candidates.md` → `research/v1.1-candidates.md`
  rename + inline annotations + carry-forward framing
  refinement: ~30-45 min
- Cross-reference updates at canonical surfaces: ~10-15 min
- `v0_9-HANDOFF.md` substrate generation: ~30-60 min

**Combined ~100-180 min cycle close cycle-doc updates substeps**
per v0.7 cycle close 6-substep precedent. Bounded cycle-hygiene
work; substantively absorbed alongside per LOCK 2 substep
expansion.

---

## §3 V1.0 ship-gate criteria evolution

**Pre-v0.8 state (post-v0.7 cycle close):**
- ✓ #1 quality-axis parenthetical (CLOSED at v0.5; preserved)
- △ #1 statistical-meaningful-wins PARTIAL (v0.6 8-cell subset)
- ✓ #2 onboarding pipeline (CLOSED at v0.7 SECONDARY)
- ✗ #3 external dogfood NOT MET (v0.6 Tier 3 cancellation)

**Narrative:** "2-of-3 MET + 2 carried-forward."

**Post-v0.8 state (locked):**
- ✓ #1 quality-axis parenthetical (preserved from v0.5)
- ✓ #1 statistical-meaningful-wins MET via v0.8 Cluster 1
  Stream B matrix-completion full 24-cell × n=5 × 2 conditions
  + F9 tag-AND-control discipline (UPGRADED from △ PARTIAL)
- ✓ #2 onboarding pipeline (preserved from v0.7; substantively
  completed at v0.8 via /prime-atlas Skill substrate as third
  Skill in cohort-onboarding-pipeline flow)
- △ #3 external dogfood DEFERRED-BY-DESIGN — v1.0 launch
  trigger IS the cohort exposure event; recruitment
  infrastructure shipped at v0.6 + v0.7 ready for v1.0 launch-
  trigger consumption

**Narrative:** "3-of-3 MET + 1 deferred-by-design."

**Substantively cleaner launch narrative.** v1.0 launch
substrate at v0.8 close: cohort users at v1.0 launch encounter
ContextAtlas with full statistical-wins methodology backing
(N=240 substrate vs prior N=27 + 8-cell subset), full onboarding
pipeline (init + /generate-adrs + /index-atlas + /prime-atlas),
user-trust pre-launch gates closed (A1+A2+A3 absorbed), and
substrate hygiene at substrate maturity (FO-15 + FO-16 + SDK
upgrade absorbed).

---

## §4 Methodology amendments

**Per LOCK D.3 selective absorption:**

### Absorbed at v0.8 (load-bearing for Cluster 1 matrix-completion quality)

- **F3 cell-selection empirical pre-screen** (~100-150 LOC at
  benchmarks-repo `scripts/v0.8-cell-screen.mjs`). n=2 dry-run
  trials per candidate cell before n=5 commitment.
  Substantively improves cell-selection methodology; v0.5 + v0.6
  observation: theoretical bucket-tier framing alone
  insufficient.
- **F5 variance-control auto-stretch refinement** (substrate-
  improvement at benchmarks-repo orchestration scripts). Extend
  hono h1 auto-stretch pattern to high-variance non-hono cells
  with budget-controlled limit. v0.6 observation: 4 of 5 v0.5
  anchors INCREASED variance in v0.6 substrate.
- **F9 tag-AND-control discipline** (per LOCKs D.1 + D.2). Run
  manifest substrate (~50-100 LOC) + RUBRIC.md amendment (~30-50
  LOC at cycle close). Generalizable methodology discipline;
  prevents F1-style cross-cycle confound recurrence.

### Deferred to v1.0+ post-launch analytical work

- **F1 atlas-substrate-version confound causal mechanism
  investigation** (per Adjudication 4 lock at v0.8 cycle pre-
  planning). F9 discipline at v0.8 sufficient for clean within-
  cycle data; F1 causal closure substantively v1.0+ work.

### Observation-only at v0.8

- **F2** anchor-cell attenuation direction-uniform — observation
  documenting v0.5→v0.6 pattern; no specific methodology action
- **F4** 2-axis retry policy — already absorbed at v0.6 Step
  5.3.a; no v0.8 work needed
- **F6** cross-order agreement attenuated on hallucination —
  observation documenting Sonnet position-blindness reduction
- **F7** cost-projection accuracy improved — observation
  documenting v0.6 priors validation
- **F8** F6 reproduction at higher incidence — observation

---

## §5 Explicit DEFERRED to v1.0+

Per Adjudication locks + `research/v1.1-candidates.md` substrate
inheritance (renamed at v0.8 cycle close per LOCK 2 from
`research/v0.8-candidates.md`).

**Critical discipline framing per LOCK 1:** items below are
deferred **BECAUSE non-load-bearing-to-launch** — NOT "pushed
from v0.8 to v1.1." Subtle but substantively meaningful framing
preserving LOCK 1 cycle-discipline constraint (v0.8 cycle thesis
substantively bears full launch-readiness weight; v0.9
substantively bears administrative-completion weight only; v1.1+
candidate substrate absorbs non-load-bearing items per cycle-
pacing discipline).

### Deferred to v1.0 launch trigger

- **Cohort exposure execution** — v1.0 IS the cohort exposure
  event. Recruitment infrastructure shipped at v0.6 (feedback
  template + tool-description observability + ADR-20 consent
  contract + recruitment infrastructure + B17 hybrid capture)
  ready for v1.0 launch-trigger consumption. Deferred BECAUSE
  cohort exposure is a v1.0-launch-trigger event, not a v0.8
  pre-launch substrate-generation task.

### Deferred to v1.0+ post-launch analytical/refinement cycles

- **F1 atlas-substrate-version confound causal mechanism
  investigation** (per Adjudication 4 lock). Disambiguate
  atlas-content-volume vs quality vs time-of-measurement vs
  sample variance via re-measurement + content-source ablation
  studies. Deferred BECAUSE F9 discipline at v0.8 sufficient
  for clean within-cycle matrix-completion data; F1 causal
  closure substantively v1.0+ post-launch analytical work.
- **Cohort UX refinement candidates 11-14** at
  `research/v1.1-candidates.md` Cohort UX Refinement category:
  - #11 Commit-message filter customization for contextatlas-
    on-itself dogfood
  - #12 FO-9 PowerShell wrapper stderr noise
  - #13 FO-11 generate-adrs --overwrite flag
  - #14 FO-14 Skills-substrate-currency gap

  Deferred BECAUSE post-launch refinement work — surface
  observability for cohort UX gaps materializes post-launch via
  actual cohort usage signal, not pre-launch via speculative
  engineering. Load-bearing-to-launch discipline preserved per
  LOCK 1 constraint.
- **Substrate-evolution depth candidates 1-7** at
  `research/v1.1-candidates.md` Substrate Evolution category
  EXCEPT #5 partial-absorbed:
  - #1 β-3 empirical CLI cost+depth measurement
  - #2 Reference-context substantive-contribution controlled A/B
  - #3 CLI multi-call investigation orchestrator pattern
  - #4 validate-atlas auto-invocation at index subcommand
  - #5 contextatlas-on-itself Skill cohort entry path empirical
    validation — **PARTIALLY ABSORBED at v0.8 Cluster 3** via
    /prime-atlas Skill empirical validation at contextatlas-on-
    itself dogfood substrate
  - #6 /update-atlas Skill substrate (CONTINGENT)
  - #7 Cross-ADR linkage emergence empirical confirmation at
    CLI surface

  Deferred BECAUSE non-load-bearing-to-launch — substantive
  substrate-evolution work bounded at canonical engineering
  surface; v1.0 launch substrate substantively complete without
  these items per LOCK 1 discipline constraint.
- **Test substrate candidate 15** (Flaky LSP-interaction tests
  inventory + race-condition triage) at
  `research/v1.1-candidates.md` Test Substrate category.

  Deferred BECAUSE non-load-bearing-to-launch — test substrate
  maintainability is post-launch work; pre-launch test signal
  substantively bounded via re-run-PASS pattern at v0.7 cycle
  precedent.
- **Cross-cycle inheritance items CC1-CC3** at
  `research/v1.1-candidates.md` Cross-cycle inheritance
  category:
  - CC1 Cross-model empirical measurement (single-judge-model
    limit; v1.0+ matrix-completion refinement candidate)
  - CC2 recommended_model SKILL.md frontmatter (Claude Code
    feature dependency)
  - CC3 Skill pre-flight model-detection (Claude Code API
    dependency)

  Deferred BECAUSE Claude Code feature dependencies +
  substrate-orthogonal to v1.0 launch readiness.

### Deferred to v1.1+ feature-unlock cycles

- **Task-shaped bundle queries** (per ROADMAP Key efficiency
  unlocks table; v0.5+ flexible-placement + v0.8+ candidate per
  ROADMAP v0.8+ block):
  - `why_does_this_fail(symbol, error)`
  - `onboard_to_feature(feature)`
  - `audit_change(diff)`

  Deferred BECAUSE feature-unlock-bearing (~2-4+ weeks
  substantive scope); doesn't gate v1.0 launch; v1.1+ as
  efficiency-collapse-feature-unlock cycle is natural placement
  per ROADMAP positioning.
- **Progressive disclosure with stable IDs** (per ROADMAP Key
  efficiency unlocks table; v0.6+ Planned status)
- **Hot-path learning** (per ROADMAP Key efficiency unlocks
  table; v0.6+ Planned status)
- **Semantic embedding layer for `find_by_intent`** (evidence-
  gated per ADR-09 framing)
- **Additional language adapters** (Rust, C#, Java by demand)

### Out of roadmap

- VS Code extension
- Web dashboard for index inspection

---

## §6 Wall-clock + cost envelope

**Wall-clock estimate per LOCK H:** ~3-4 weeks total per Option
γ ordering (main-repo sequential + benchmarks-repo parallel
Stream B cadence).

**Per-cluster breakdown:**

| Cluster | Scope | Wall-clock | Parallelization |
|---------|-------|------------|-----------------|
| Cluster 1 — Stream B matrix-completion | Full 24-cell × n=5 × 2 + F3+F5+F9 amendments + run manifest substrate | ~2-3 weeks benchmarks-repo execution | PARALLEL with clusters 2-4 |
| Cluster 2 — TERTIARY A1+A2+A3 | A1 first → A2+A3 paired | ~3-5 cycle days main-repo | Sequential within main-repo |
| Cluster 3 — /prime-atlas Skill | SKILL.md substrate ~200-300 LOC + tool-call probe + tools-intro prompt + tests | ~3-5 cycle days main-repo | Sequential within main-repo |
| Cluster 4 — Mechanical absorption | SDK first → FO-15 + FO-16 paired | ~3-5 cycle days main-repo | Sequential within main-repo |
| ROADMAP.md update | Revision history + v0.8+ block + v1.0 narrative | ~30-60 min main-repo | Cycle close substep |
| Cycle close + ship gate | Step 5-style ship gate per v0.7 6-substep precedent | ~3-4 cycle days main-repo | Cycle close |

**Cost envelope per LOCK C:**

- Cluster 1 Stream B matrix-completion: ~$200-400 (extrapolated
  from v0.6 cost-priors at 8-cell subset scaling to 24-cell;
  240 trials × judge-grading + retries)
- Main-repo clusters 2-4: minimal API spend (Cluster 4 SDK
  upgrade no API; Cluster 2 TERTIARY no API; Cluster 3 /prime-
  atlas no API at engineering time; empirical validation run
  may surface bounded API call for tool-call probe at dogfood
  substrate)
- ROADMAP update: zero API spend (manual doc work)
- Cycle close + ship gate: minimal API spend

**Total cycle envelope:** ~$200-400.

**Honest scope-locking disclaimer.** Wall-clock estimates are
cycle-pacing planning only per Travis Lock 2 no-wall-clock-
ceiling discipline. If mid-cycle substantive scope adjustment
surfaces (e.g., Stream B scope shape compression at C-adjudication
escape valve, SDK migration scope expansion, /prime-atlas
substrate iteration surface beyond Option β bounds), envelope
refines per substantive cycle-pacing discipline.

---

## §7 Locked sequence

**Substep cluster ladder per v0.5/v0.6/v0.7 9-step canonical
inheritance.** Per-cluster substep ladder firms at cluster-start
design adjudication substep (Q-pattern Q1.0 / Q2.0 / Q3.0 / Q4.0
per cluster).

**Cluster sequence per Option γ ordering:**

1. **Cluster 1 — Stream B matrix-completion** (parallel
   benchmarks-repo cadence; starts at cycle open)
   - Step 1.0 — Design adjudications (matrix-completion methodology
     locks at benchmarks-repo; F3 + F5 + F9 amendment substrate
     design; cell selection per F3 dry-run pre-screen)
   - Step 1.1+ — Cell selection + dry-run pre-screen (F3)
   - Step 1.2+ — Run manifest substrate engineering (F9 D.1)
   - Step 1.3+ — Matrix execution (240 trials with F5 variance-
     control)
   - Step 1.4+ — Paired-t cross-cell rollup analysis per ADR-19
     §4
   - Step 1.5+ — Phase-11 reference doc generation
   - Step 1.X — Cluster close

2. **Cluster 2 — TERTIARY A1+A2+A3** (main-repo sequential start)
   - Step 2.0 — Design adjudications (TERTIARY scope detail; A1
     classifier branch design; A2+A3 idempotency model design)
   - Step 2.1 — A1 absorption
   - Step 2.2+ — A2+A3 paired absorption
   - Step 2.X — Cluster close

3. **Cluster 3 — /prime-atlas Skill substrate** (main-repo)
   - Step 3.0 — Design adjudications (SKILL.md structure;
     tool-call probe substrate design; tools-introduction prompt
     scope)
   - Step 3.1 — SKILL.md substrate generation
   - Step 3.2 — Tests + empirical validation at contextatlas-on-
     itself
   - Step 3.X — Cluster close

4. **Cluster 4 — Mechanical absorption** (main-repo)
   - Step 4.0 — Design adjudications (SDK migration scope; FO-15
     + FO-16 validate-atlas substrate design)
   - Step 4.1 — SDK upgrade ^0.27.0 → ^0.32.0
   - Step 4.2+ — FO-15 + FO-16 paired absorption
   - Step 4.X — Cluster close

5. **ROADMAP.md update** (cycle close substep)
6. **Ship gate (Step 5-style cycle close per v0.7 6-substep
   precedent)**

**9-step locked sequence (v0.5+ canonical inheritance) preserved
at ship gate:**

1. Pre-flight verification (npm test main + benchmarks + atlas
   refresh sanity)
2. Apply working content (doc updates per ship discipline)
3. Stage explicit-paths
4. Create ship commit via HEREDOC
5. Verify commit landed
6. Create annotated tag v0.8.0 via HEREDOC per v0.5/v0.6/v0.7
   SHA-free precedent
7. Verify tag created
7.5. Post-execution verification (canonical Step 7.5
   inheritance): inspect committed body + tagged body for
   HEREDOC escape artifacts; encoding issues; formatting drift;
   cross-document SHA reference accuracy. STOP if artifacts
   caught; apply Path X amend + tag re-create per pre-push
   window affordance (Class-15 instance 17 v0.7 cycle precedent).
8. Capture ship-commit SHA
9. Cross-repo back-reference at separate commit + cycle close
   commit per v0.5 + v0.6 + v0.7 ship-gate precedent

**Unblocks.** V0.9 administrative cycle (repo hygiene + doc
reorganization + npm deployment) preceding V1.0 public launch
trigger; launch-document publication post-v1.0 ship per Q-pre-6
lock + v0.7-SCOPE.md inheritance (Travis-readiness timeline; not
blocking).

**Critical cycle-discipline constraint preserved at §7 locked
sequence surface per LOCK 1.** v0.9 cycle substantively bears
administrative-completion weight only; NO carry-forward work
from v0.8 substep clusters 1-4 + cycle-doc updates substeps.
Load-bearing-to-launch items in v0.8 cycle scope substantively
MUST complete in v0.8 — non-negotiable per launch-readiness
discipline. Non-load-bearing-to-launch items carry forward to
`research/v1.1-candidates.md` substrate per LOCK 2 backlog file
disposition (NOT to v0.9).

---

## §8 Honest scope-locking disclaimer

V0.8-SCOPE.md substantively a scope-doc — not a launch document
and not a v1.0-release-bearing commitment. Scope adjudications
A-H locked at v0.8 cycle pre-planning per Travis adjudication;
substep cluster ladders firm at cluster-start design
adjudication substep per established Q-pattern.

**Inheritance from v0.5/v0.6/v0.7 cycle pre-planning pattern:**

- **F2-sequencing-style per-cycle thesis discipline** preserved
  — v0.8 cycle adjudicates its own scope thesis per established
  cycle pre-planning pattern; this scope-doc is a scope-doc-
  for-this-cycle, not a prescription for v0.9+ or v1.0+.
- **Travis Lock 2 no-wall-clock-ceiling discipline** preserved
  throughout cycle. Wall-clock estimates at LOCK H + §6 are
  cycle-pacing planning only; substantive cycle-pacing
  discipline preserved per substantive cycle-pacing surface
  observations at substep cluster boundaries.
- **Cycle-execution-time discipline** observations carried
  forward from v0.7 cycle close (15 Class-15 capstone
  composition + 2 cycle-close-emergent instances per Option C
  lock; final count 17 at v0.7 cycle close). v0.8 cycle may
  surface additional cycle-execution-time observations per
  substep cluster maturity; Class-18 trajectory disposition per
  Travis discretion at substep cluster close surfaces.
- **F9 tag-AND-control discipline** absorbed at v0.8 per LOCKs
  D.1 + D.2; canonical methodology requirement at RUBRIC.md
  amendment surface; future cycles inherit.
- **Atomic ship-gate discipline** preserved per Adjudication 3
  inheritance from v0.7 (local-only state allows clean fix
  pre-push window affordance; Class-15 instance 17 v0.7 cycle
  cosmetic remediation precedent inherited).
- **Honest scope-narrative discipline** preserved across §1-§7
  framings; deferred-to-v1.0+ items honestly framed; "3-of-3
  MET + 1 deferred-by-design" narrative shift honestly
  positioned vs current post-v0.7 "2-of-3 MET + 2 carried-
  forward" framing.

**V0.9 administrative cycle + V1.0 launch positioning post-v0.8:**

V0.8 cycle close substantively positions v0.9 administrative
cycle (repo hygiene + doc reorganization + npm deployment) as
next cycle, with v1.0 public launch trigger following v0.9 cycle
close. v0.9 cycle scope content NOT in v0.8 cycle adjudication
surface; locked at v0.9 cycle pre-planning per established cycle
pre-planning pattern.

**Critical cycle-discipline constraint per LOCK 1.** v0.9 cycle
substantively bears administrative-completion weight only; NO
carry-forward work from v0.8. Load-bearing-to-launch items in
v0.8 cycle scope (Stream B matrix-completion + TERTIARY A1+A2+A3
+ /prime-atlas Skill + Mechanical absorption + ROADMAP cycle
close cycle-doc updates substeps) substantively MUST complete in
v0.8 — non-negotiable per launch-readiness discipline. Non-load-
bearing-to-launch items substantively carry forward to
`research/v1.1-candidates.md` substrate (NOT to v0.9) if mid-
cycle scope-compression pressure surfaces. Discipline rationale:
keeping v0.9 framing private at v0.8 cycle thinking prevents
substrate-erosion risk where v0.8 cycle work substantively
softens because "v0.9 will catch it."

V1.0 launch trigger executes against:

- 3-of-3 MET V1.0 ship-gate criteria substrate
- Full cohort-onboarding-pipeline (init + /generate-adrs +
  /index-atlas + /prime-atlas at 4 cohort entry surfaces)
- N=240 Stream B matrix-completion statistical-wins substrate
- F3 + F5 + F9 methodology amendments at canonical RUBRIC.md
  surface
- Recruitment infrastructure ready (v0.6/v0.7-shipped feedback
  template + tool-description observability + ADR-20 consent
  contract + recruitment infrastructure)
- User-trust pre-launch gates closed (A1+A2+A3 absorbed)
- Substrate hygiene at substrate maturity (FO-15 + FO-16 + SDK
  upgrade absorbed)
- ROADMAP.md substrate-currency at canonical surface (v0.5/v0.6/
  v0.7/v0.8 absorption reflected)

V1.0 launch is the cohort exposure event; ContextAtlas v1.0
ships into actual cohort usage rather than into more pre-launch
validation cycles.

---

## Revision history (v0.8-SCOPE.md)

- **2026-05-12 (cycle pre-planning draft)** — initial scope-doc
  draft per Direction D' refined lock + 4 substep cluster
  framework (LOCKs A-H at v0.8 cycle pre-planning design
  adjudication surface). Substrate-inheritance bridge from v0.7
  launch-bearing cycle close (`v0_8-HANDOFF.md` commit
  `49ca82c`) to v0.8 cycle execution.
