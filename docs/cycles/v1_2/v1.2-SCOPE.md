# v1.2-SCOPE.md — Automatic atlas freshness (tiered refresh)

**Status:** Scope-doc drafted 2026-09-24 at v1.2 cycle re-entry
(post-v1.1.3; roughly 2.5 months of inactivity since the prior v1.2
federation draft). Supersedes the 2026-06-09 federation-first v1.2
draft, which is retargeted to
[`../v1_3/v1.3-SCOPE.md`](../v1_3/v1.3-SCOPE.md).

**Why the re-sequence.** Travis locked a use-case pivot on 2026-09-24.
ContextAtlas becomes the **project-brain layer** under a new
workspace command center, built in a separate repo:

```
Workspace command center   (new repo: UI, project/session manager, launcher)
        │
ContextAtlas  +  Session Memory   (new repo: transcript indexer, checkpoints)
        │
CCR (claude-code-router)   (routing, providers, credential pools)
```

A warm-start session is only as good as the atlas it reads. Today
the atlas goes stale silently. Nothing triggers a refresh, and the
running MCP server never re-reads `atlas.json`. The dogfood atlas
was 17 commits behind HEAD at cycle entry. Automatic freshness is
therefore the prerequisite for everything above it, and it is the
smallest change with daily value.

---

## §1 Cycle thesis

v1.2.0 keeps the atlas current **without anyone having to remember
to run anything** and **without becoming a token drain**. Refresh is
split into three cost tiers:

| Tier | What | Cost | Trigger |
|---|---|---|---|
| **0: structural** | LSP symbol inventory, stale-symbol pruning, git signal, SHA diff → pending-source queue | Free (no model calls) | Git hooks: post-commit / post-merge / post-checkout |
| **1: incremental claims** | Re-extract claims for **pending sources only** | Subscription (headless `claude -p` running the `/refresh-atlas` skill); batched + capped | `pre-push` by default; nightly schedule available; manual |
| **2: full / first-time** | Existing `contextatlas index` / `--full` | API | Manual (init recommends API for first extraction; API quality > Skill path per v0.8 65-83% claim-ratio finding) |

The MCP server hot-reloads `atlas.json`, so refreshed state is visible
without restarting sessions. ADR-02's zero-query-time-API-call
invariant is untouched: every tier runs at index time.

---

## §2 Cycle-entry fresh-read findings (dev, 2026-09-24)

These are verified against source and change the scope.

- **F-1: stale symbols are never pruned.** Stage 0 imports the
  committed atlas (`clearSymbols` + re-insert), and Stage 4 only
  *upserts* the fresh inventory. Stage 5 deletes symbols only for
  deleted **prose** paths (`diff.deleted` comes from the prose walk).
  Symbols from deleted, renamed or moved **source** files, and
  symbols removed from files that still exist, therefore persist in
  `atlas.json` indefinitely. That was tolerable for manual full-ish
  runs but is fatal for a structural-refresh tier whose whole job is
  structural truth. **Fix is Phase 1 and a prerequisite for Tier 0.**
  (`src/extraction/pipeline.ts` Stages 0/4/5;
  `src/storage/symbols.ts` `deleteSymbolsByPath` / `clearSymbols`.)
- **F-2: the shipped CLI `index` extracts ADR/docs claims only.**
  Docstring extraction (`extractDocstringsForFile`, `pipeline.ts`)
  and commit-message extraction (`extractCommitMessagesForRepo`,
  `commit-message-extractor.ts`) are exported library functions.
  They are called only from `scripts/dogfood-extract.mjs` (header
  says "production-grade CLI integration with full three-stream
  support is v0.5+ work", never absorbed) and from benchmarks-repo
  scripts. The `/index-atlas` Skill path does walk all three
  streams. Consequences:
  - Tier 2 "API is higher quality" holds today only for the ADR
    stream via the shipped CLI.
  - Incremental docstring refresh has no CLI-side precedent.

  **Three-stream CLI `index` becomes Phase 2.** Decision point D-1
  (below) covers the cost-default question.

  **Closed in Phase 2** (see the Phase 2 outcome): `contextatlas index`
  extracts ADR/docs, docstring and commit streams, gated by
  `extraction.streams`. Retiring `scripts/dogfood-extract.mjs` waits
  for the paid parity run.
- **F-3: source-SHA tracking is per stream.** Prose files are keyed
  by relPath. Docstrings are keyed per source file
  (`setSourceSha(db, relPath, fileSha)` at `pipeline.ts:877` at cycle
  entry; since Phase 2 in `extractDocstringFile` in
  `src/extraction/docstring-stream.ts`, inside the file's write
  transaction). Commits are keyed `commit:<sha>`. The pending-source
  queue must model all three kinds.

Added 2026-09-24 at Phase 1 entry. F-4 and F-5 were found by the
orchestrator's fresh read and reproduced on real data by the Phase 1
reproducer. F-6 to F-8 were found during Phase 1 implementation.

- **F-4: Stage 5 is not stream-aware (fixed in Phase 1).**
  `diffShas(proseFiles, committedShas)` (`src/extraction/file-walker.ts`)
  marked every `source_shas` key missing from the **prose** walk as
  deleted. That included every docstring key and every commit key.
  Stage 5 then ran `deleteClaimsBySourcePath`, `deleteSymbolsByPath`
  and a `source_shas` delete for each of them.
  - **Affected atlases.** Running CLI `contextatlas index` on any
    three-stream atlas (Skill-built, or built by
    `scripts/dogfood-extract.mjs`) deleted:
    - every docstring claim and every commit claim;
    - the freshly upserted symbols of every docstring-keyed file,
      and with them the ADR claims' links into those files.
  - **The in-code comment was wrong.** The LOCK 2.b comment at Stage
    5 ("commit claims themselves survive") never held for commit
    keys in `source_shas`.
  - **Real-data reproduction** (v0.4 dogfood atlas at 454fcc8, prose
    unchanged, zero API calls; session scratchpad `p1-e2e/`):
    - docstring claims 377 → 0;
    - symbols 768 → 219;
    - `source_shas` 80 → 24;
    - ADR links into the 56 docstring-keyed files 1423 → 0;
    - injected commit claims (3 `commit:<sha>` + 3 bare-sha) 6 → 0.
  - **Silent.** With modern `adr:` prose sources the run exited 0,
    and `validate-extraction` reported "conforms (449 claims across 24
    sources)".
  - **Priority.** Tier 0 (Phase 3) runs Stages 0–5 on every commit,
    so F-4 had to be fixed before it.
- **F-5: commit source keys diverge between paths (record only; Phase
  2).** Both paths set claim `source` to `commit:<sha>`. They differ in
  `source_path` and the `source_shas` key:
  - the CLI commit-message extractor uses `commit:<sha>`
    (`commit-message-extractor.ts`);
  - the Skill uses the bare 40-hex sha (`.claude/skills/index-atlas/
    SKILL.md` Phase B step 3).

  Phase 1 recognizes both forms. Normalizing them belongs to the
  three-stream CLI (Phase 2), which must also migrate existing keys.

  **Closed in Phase 2** (L-2). `commit:<sha>` is the canonical key and
  `source_path` on both paths. Every `contextatlas index` migrates bare
  keys and claim paths (`normalizeCommitKeys`, Stage 0.5), keeping the
  `commit:` form when a sha exists in both. SKILL.md writes the
  canonical form from `list-extraction-sources`' new
  `commit.source_key`, and readers accept both forms. `validate-atlas`
  warns on the bare form, and `doctor` flags stale installed skills.
- **F-6: `resolve-symbols` leaves dangling claim links (fixed in
  Phase 1).**
  - **Cause.** The Skill-path symbol writer rebuilds `symbols[]`
    wholesale. It also merges each claim's prior `symbol_ids` into the
    new resolution, and it skips claims with no candidates entirely.
  - **Effect.** A link to a symbol whose file was deleted survives, and
    the written `atlas.json` then fails to import with `FOREIGN KEY
    constraint failed`, because `claim_symbols.symbol_id` references
    `symbols.id` with foreign keys ON. That breaks both the MCP server
    and `index`. The wholesale rebuild also drops the symbols of any
    file whose listing failed.
  - **Evidence.** Reproduced by
    `src/extraction/cli-resolve-symbols.test.ts` (real tsserver: resolve,
    delete a file, resolve again) and by a direct `importAtlas` probe.
- **F-7: `claims[].symbol_candidates` does not survive a CLI `index`
  (closed in Phase 2; see below).** The SQLite `claims` table has no column
  for it, so an import → export round trip drops it; this was checked
  with a direct probe. Any CLI `index` run on a Skill-built atlas
  therefore strips the raw candidates that `resolve-symbols` would
  need for a later re-resolution. The hook-driven Tier 0 re-export
  planned for Phase 3 would strip them on every run.

  **Closed in Phase 2** (L-5). Local cache migration 6 adds
  `claims.symbol_candidates`. The importer and exporter carry it; the
  exporter emits it after `symbol_ids`, only when non-empty. CLI
  extraction now writes it for all three streams. It is read only by
  the exporter, never by MCP tool output. Checked on the three v0.8
  Skill atlases: every non-empty list survives import → export
  (cobra 163, hono 253, httpx 204 claims); 11 empty lists (1 + 10 + 0)
  export as an absent key.
- **F-8: `resolve-symbols` walks without `exclude_pattern` (open;
  Phase 3 decision).** `resolve-symbols` calls `walkSourceFiles`
  without exclude patterns. `index` and `list-extraction-sources` do
  apply them. The two paths therefore produce different symbol sets:
  the Skill path includes test-file symbols, and a CLI `index` (Phase
  1 prune rule 4) removes them again. That churn would show up in
  `atlas.json` diffs once Tier 0 runs automatically. Phase 1 left the
  walk scope unchanged; it changes the Skill path's output, so it
  needs a decision.

---

## §3 Scope — in

### Phase 0: SDK currency (Arc 4 carry-forward)
- **MCP SDK** 0.5.0 → either 1.30.1 or the v2 split package
  `@modelcontextprotocol/server`. The target is picked by the v2
  sandbox spike: v2 if tests pass, tools/list is byte-identical and
  protocol negotiation works with Claude Code; else 1.30.1.
  - Clears GHSA-w48q-cv73-mx4w (the only advisory npm audit reports
    on 0.5.0).
  - Update to the Arc 4 row: GHSA-8r9q-7v3j-jr4g's current range
    (advisory last updated 2026-07-15) is ≥1.3.0 <1.25.2, which
    excludes 0.5.0. The form-data chain (via `@anthropic-ai/sdk`
    0.32.x → `@types/node-fetch`) was already patched in range,
    4.0.5 → 4.0.6 (GHSA-hmw2-7cc7-3qxx), by 618ce89's
    `npm audit fix`; the upgrade removes form-data entirely.
  - 1.30.1 needs no source changes but adds ~80 transitive packages
    (express, hono, jose…) never loaded on the stdio path, and its
    low-level `Server` is `@deprecated`.
- **`@anthropic-ai/sdk`** 0.32.1 → 0.128.0. This removes two
  deprecation warnings: `node-domexception` at install, and the Node 22
  `punycode` (DEP0040) warning at startup.
- **Pre-existing bugs found by the Phase 0 research, fixed in this
  phase:**
  - (a) Error classes were imported from `@anthropic-ai/sdk/error.js`,
    which resolves to the CJS build, while the client throws ESM
    errors. `instanceof` never matched, so the extraction and judge
    retry wrappers were dead code in production, and generate-adrs
    never mapped SDK errors to remediation messages. Fix: import from
    the package root and classify by HTTP status (so clients built by
    another package copy, e.g. benchmarks scripts, also classify).
  - (b) `APIError.headers` is now a web `Headers`, so the Retry-After
    parsing is updated.
  - (c) The CLI generate-adrs call sent
    `thinking: {type:"enabled", budget_tokens: 32000}`, which
    claude-opus-4-7 rejects with a 400. It also used a non-streaming
    `max_tokens: 64000`, which SDK 0.128.0 refuses client-side
    (`calculateNonstreamingTimeout`: more than 10 min expected).
    Fix: `thinking: {type:"adaptive"}` +
    `output_config: {effort:"xhigh"}` via
    `messages.stream().finalMessage()`. `max_tokens` stays 64000, and
    an explicit error fires on `stop_reason: "max_tokens"`. The CLI
    now runs at the same `xhigh` effort level as the Skill
    `effort: xhigh` pin (API-parameter layer; the Skill path's
    thinking and `max_tokens` settings are not verified to match).
    Recorded as an appended ADR-02 amendment.
  - (d) The judge sends `temperature: 0`, which claude-opus-4-7
    rejects with a 400. Omit it for models that reject sampling
    params.
  - (e) `dist/` was never cleaned before a build, so the published
    tarball carries orphaned `cli-show-*` modules. The build now
    cleans first.
- **Retry ownership (Travis, 2026-09-24):** the wrapper owns retries.
  SDK `maxRetries: 0` on the extraction and judge clients.
  generate-adrs (no wrapper) keeps the SDK default of 2. Clients
  pass `authToken: null` so `ANTHROPIC_AUTH_TOKEN` in the environment
  doesn't add a second credential header.
- The extraction request itself is unchanged: claude-opus-4-7 with no
  `thinking` field is valid. The ADR-02 frozen substrate is untouched.
  claude-opus-4-7 status is Active (previous-generation), not
  deprecated.

#### Phase 0 outcome (2026-09-24): committed in 227f2f4 (code) + ad16cae (docs)

**MCP decision: v2.** The sandbox spike met all three pre-set
criteria:
- tests pass;
- `tools/list` is byte-identical to 0.5.0;
- negotiation works with Claude Code's default stdio mode. This was
  checked by static analysis of the Claude Code binary and by
  emulation with the v2 client's legacy mode. It was not tested in a
  live session.

What shipped:
- `@modelcontextprotocol/server` pinned exactly to `2.1.0` as the
  runtime dependency. `@modelcontextprotocol/client` and
  `@modelcontextprotocol/core` `2.1.0` are exact-pinned test-only
  devDependencies.
- The low-level `Server` and the hand-written JSON Schema tools are
  kept. zod is transitive only and is never imported.
- `server.connect(new StdioServerTransport())` stays hand-wired.
  `serveStdio` and the 2026-07-28 era are deferred to Phase 4 (see
  the "Server wiring" bullet there).

Why v2 over `@modelcontextprotocol/sdk` 1.30.1:
- **Footprint.** A fresh install of ContextAtlas's full runtime tree is
  87 packages on v2 against 162 on 1.30.1 (97 on 0.5.0). This is the
  spike's `--omit=dev --omit=peer` measurement.
- **No HTTP frameworks.** 1.x installs express, hono,
  @hono/node-server, cors, express-rate-limit and ajv as hard
  dependencies. That runs against the CLAUDE.md "no HTTP frameworks"
  constraint.
- **Support window.** Upstream says v1.x gets fixes for at least 6
  months after v2 GA (2026-07-27), so roughly to 2027-01. That quote
  comes from a WebFetch summary of the upstream README; the verbatim
  text was not checked. Moving to 1.x now would mean a second
  migration.
- **Counterweights.** v2 is young: 2.0.0 GA was 2026-07-27, and 2.1.0
  came out 2026-09-23. That is the reason for the exact pin. `Server`
  is `@deprecated` in both lines.
- **Measured afterwards.** On the real lockfile after both SDK
  upgrades, runtime (non-dev) entries went from 99 to 57.

**Anthropic SDK, as implemented** (see the ADR-02 2026-09-24
amendment):
- **Version.** `^0.128.0`, resolving to 0.128.0.
- **Error classes.** Imported from the package root. Classification
  lives in `src/extraction/retry-policy.ts`, with a local copy at
  `src/grading/retry-policy.ts`. It checks class identity first, then
  falls back to the error's shape for foreign SDK copies.
- **Single retry layer.** The extraction and judge wrappers send every
  request with the per-request option `{ maxRetries: 0 }`.
  - The retry decision follows SDK `shouldRetry`, including
    `x-should-retry`. 408 and 409 now retry.
  - Retry-After is read from web `Headers` or a plain record, and
    capped at 30 s.
- **generate-adrs.**
  - Streams with adaptive thinking at effort xhigh.
  - Raises a truncation error on `max_tokens` /
    `model_context_window_exceeded`.
  - Maps interrupted streams.
  - Gives re-run advice only for transient errors.
  - Maps 401/403 to exit 2.
- **Judge.** Omits `temperature` for claude-opus-4-7.
- **authToken.** Both clients built in `src/` pass `authToken: null`.

**Gate evidence (working tree, 2026-09-24):**

Build and type checks:
- **Typecheck.** `npm run typecheck` exits 0.
- **Typecheck including test files.** 46 errors (with
  `noUnusedLocals`/`noUnusedParameters` off, matching the baseline).
  That is the same set as the pre-change baseline captured in the
  session scratchpad (`research-mcp/baseline-tsc-errors.txt`) when
  compared without line numbers: 0 new, 0 removed.
- **Build.** `npm run build` succeeds: it cleans `dist/`, then runs tsc
  and `generate-prompt-artifacts`.
- **Tarball.** `npm pack --dry-run` lists 423 entries:
  - 0 `cli-show-*` orphans (the published 1.1.3 tarball carries 8);
  - 0 test files.

Tests:
- **Full vitest.** 102 files and 1866 tests. That is the 1766 tests
  at HEAD (96 files), plus 94 from the Anthropic work, plus 6 from the
  MCP port and fixes.
  - Runs are clean except for the Windows `EBUSY` rmdir flake in
    subprocess-test `afterEach` cleanup: 0–4 failures per run.
  - Every failing file passes when rerun alone. One of the recorded
    runs was 1866/1866.
- **MCP tests.** `src/mcp/server.test.ts` + `src/index.test.ts`:
  70/70.
- **Mutation checks.** Each fix has a regression test, verified by
  mutation:
  - reverted `error.js` import (6 failures);
  - dropped `authToken` / `maxRetries`;
  - removed the stream-interrupt mapping;
  - wrong config key in the truncation message;
  - unconditional re-run advice;
  - one-size truncation text;
  - `x-should-retry` ignored;
  - zero Retry-After accepted;
  - stdin-EOF shutdown;
  - uninitialized-handshake warning.

MCP wire probes:
- **tools/list.** Byte-identical to the 0.5.0 build: 4069 B, sha256
  prefix `34a88ce769fe1cec`.
- **Tool results.** Successful results are identical in compact and
  json formats.
- **Protocol negotiation.** The server echoes 2024-11-05, 2025-03-26,
  2025-06-18 and 2025-11-25. For 2026-07-28 sent via `initialize` it
  answers 2025-11-25. The SDK's supported list also includes
  2024-10-07; a later probe of the working-tree build confirmed it is
  echoed too.
- **Client matrix.** Against the ported build, the MCP 0.5.0 client
  (benchmarks copy) and the v2 client in legacy and auto modes pass
  list/call/error-code checks. The 1.30.1 client passed against the
  spike build.

Security and dependencies:
- **npm audit.** `npm audit --omit=dev` goes from 3 high to 1 high.
  - Cleared: GHSA-w48q-cv73-mx4w, and `brace-expansion`
    GHSA-mh99-v99m-4gvg / GHSA-rgw5-rvv9-x895.
  - Remaining: `js-yaml` 4.3.0.

**Wire-visible changes** (recorded in CHANGELOG [Unreleased]):
- the negotiated protocol version now echoes the client (up to
  2025-11-25);
- `MCP error -326xx: ` is gone from `error.message`;
- a malformed `tools/call` returns -32602 instead of -32603;
- `isError` results serialize with key order `{content, isError}`;
- stdin EOF now shuts the server down and exits 0, and requests still
  in flight are not answered;
- startup and client-initialized log lines changed.

**Follow-ups (not done in Phase 0):**
- **EBUSY flake.** The Windows `EBUSY` tmpdir flake (Arc 4 row;
  `v1_1-HANDOFF.md` "EBUSY tmpdir Windows flake") hit
  `cli-list-extraction-sources`, `cli-resolve-symbols`,
  `src/extraction/cli-runner` and `src/generation/cli-runner` this
  phase, all in `rmdir` during tmpdir cleanup. A likely fix is
  `rmSync(..., { maxRetries, retryDelay })` in those `afterEach`
  hooks.
- **Network-hitting test.** The `cli-runner` "propagates --yes" test
  in `src/generation/cli-runner.test.ts` still calls the real API with
  a bogus key; it accepts exit 1 or 2. It could be made hermetic with
  the `vi.stubGlobal("fetch")` pattern.
- **Cost estimator.** Recalibration is deferred and needs approval:
  - the pre-flight output bound of 75k is above `max_tokens` 64k;
  - the estimator has never modeled thinking tokens, which are billed
    as output under both the v0.7 enabled-thinking config and the
    v1.2 adaptive config;
  - the Opus 4.7 tokenizer likely makes chars/4 an underestimate.
- **Dogfood atlas.** `.contextatlas/atlas.json` still carries obsolete
  claims:
  - the `budget_tokens` 32k parameter and the SDK-cast rationale;
  - "McpError InvalidParams";
  - a v0.8-SCOPE claim that "SDK 0.32+ natively types the `thinking`
    parameter", which v0.8 Step 4.1 found untrue for 0.32.x.

  Re-indexing costs money on the CLI path and needs approval.
- **No live smoke yet.** No live API call has been made for
  `generate-adrs` (adaptive + xhigh + streaming). A one-shot paid
  `contextatlas generate-adrs --yes` on a tiny fixture would confirm
  there is no 400 and that usage and cost are populated.
- **Dev-only scripts.** Five `scripts/*.mjs` construct
  `new Anthropic()` without `authToken: null`:
  - `dogfood-extract.mjs`
  - `v0.5-step2-probe.mjs`
  - `v0.5-step6-within-judge.mjs`
  - `v0.5-step8-grading-harness.mjs`
  - `v0.6-step5.3-grading-harness.mjs`

  They are not shipped, and they hand their clients to the wrappers,
  so retries are still single-layer.
- **js-yaml.** Fixed right after Phase 0 in b6729d3 (4.3.0 → 4.3.2,
  lockfile-only). `npm audit --omit=dev` now reports 0
  vulnerabilities. The remaining advisories are dev-only (vitest 2.x
  chain: @vitest/mocker, esbuild, nanoid, postcss). Fixing them needs
  a breaking vitest major bump; that is a separate Arc 4 candidate.
- **CLAUDE.md. Done 2026-09-24 under Travis's approved Phase 0
  decisions.**
  - The "Critical Constraints" dependency list and the "MCP SDK"
    bullet now name `@modelcontextprotocol/server` (exact-pinned v2).
  - A new bullet records the root-import rule for Anthropic SDK error
    classes.
  - "Generation cost framing" current-state text and the Framing 1
    "CLI single-shot" line now describe adaptive thinking at effort
    `xhigh`, streamed. The historical v0.7-outcome narrative is
    untouched (Pattern 3).
- **ADR-02 permitted-modules list.** ADR-02 §Decision item 1 and the
  §Consequences grep rule name only `src/extraction/` and
  `src/grading/` as permitted to import `@anthropic-ai/sdk`.
  `src/generation/generators/anthropic-api-direct.ts` has imported it
  since v0.7 Step 2.2.a.2 (6813eb5), so that grep returns one match.
  This predates Phase 0 and is recorded in the ADR-02 2026-09-24
  entry; extending the list is an ADR decision left open.
- **Lint.** `npm run lint` is broken at baseline (no
  `eslint.config`; Arc 4 ESLint v9 row), so §6 "lint per phase
  commit" is not yet runnable.

### Phase 1: structural correctness (F-1)
- Prune symbols absent from the current inventory after Stage 4, and
  cascade `claim_symbols`. Claims that lose all symbol links are
  reported as **orphaned**. ADR-sourced claims are never deleted by
  pruning, since the ADR still says what it says. Commit claims keep
  the LOCK 2.b orphan-shell retain discipline.
- Report pruned-symbol and orphaned-claim counts in the index summary.

#### Phase 1 outcome (2026-09-24): committed in 970bf60 (code) + 9e9310a (docs)

F-1 and F-4 are fixed together; F-6 is fixed as the Skill-path
counterpart. The rules are recorded in the ADR-12 2026-09-24
amendment, which also carries the stage-by-stage prose / docstring /
commit table.
- **Classification** (`src/extraction/source-keys.ts`). Each
  `source_shas` key is classified as prose, docstring or commit.
  - The claim `source` prefix decides. If a key's claims disagree, the
    stream that deletes least wins.
  - Zero-claim keys fall back to shape: a key in the prose walk is
    prose; `commit:` or a bare sha is a commit; an extension of any
    registered adapter makes a docstring key.
  - `src/adapters/registry.test.ts` pins the extension copy to the
    adapters.
- **Stream-aware Stage 5.**
  - prose: deleted iff absent from the prose walk. This now also
    applies under `--full`, which previously never deleted prose keys.
  - docstring: deleted iff the source file is gone.
  - commit: never deleted.
- **Pruning** (`src/extraction/symbol-prune.ts`, Stage 4a).
  - Five rules. Pruned: deleted files, symbols a listed file no longer
    contains, and existing files a configured adapter owns but the walk
    now excludes. Kept as unverified: files whose listing failed, and
    files whose language is not configured.
  - `buildSymbolInventory` now reports `listedPaths` / `failedPaths`.
  - `deleteSymbolsByIds` cascades `claim_symbols`. Claims are never
    deleted; claims left without links are reported as orphaned.
- **Reporting.** New result fields: `symbolsPruned`, `claimsOrphaned`,
  `orphanedClaimsBySource`, `docstringSourcesDeleted`,
  `unverifiedSymbolFiles`.
  - The CLI appends `symbols_pruned`, `claims_orphaned`,
    `docstring_sources_deleted` and `unverified_symbol_files`, plus
    `orphaned_claims_by_source` in `--json`. Existing keys and their
    order are unchanged.
  - `files_deleted` is prose-only again.
- **Export.** Stage 7 re-exports on a prune or a docstring deletion. A
  no-op run leaves `atlas.json` byte-identical.
- **`resolve-symbols`.** It applies the same rules via
  `planSymbolPrune`: prior symbols are kept only for unverified files.
  It also drops `symbol_ids` that match no symbol and reports orphaned
  claims.

**Gate evidence (working tree, 2026-09-24):**
- **Tests first.** The new tests were written before the fix and run
  against b6729d3. Every new assertion failed except one regression
  guard:
  - `pipeline-prune.test.ts`: 8 of 9 failed. The `--full` F-4 guard
    already passed, because `--full` never deleted.
  - `source-keys.test.ts`, `symbol-prune.test.ts` and
    `registry.test.ts`: the new modules were missing.
  - `symbols.test.ts`: 2 new tests; `resolver.test.ts`: 1.
  - `cli-runner.test.ts`: 3 new tests.
  - `cli-resolve-symbols.test.ts`: the dangling-link test failed with
    a claim id missing from `symbols[]`.
  - Two unit tests were added after the implementation, not before:
    `partitionSourceShas` and `reconcileAtlasSymbols`.
- **Typecheck.** `npm run typecheck` exits 0.
- **Build.** `npm run build` succeeds.
- **Typecheck including test files.** 44 errors, against the 46-error
  baseline, compared without line numbers: 0 new, 2 removed. The two
  `extractor.test.ts` result literals are now complete.
- **Full vitest.** 105 files and 1912 tests (1866 at Phase 0 plus 46
  new). There were four full runs: two before the last comment-only
  source edits and two on the final tree.
  - **Pass counts.** 1910, 1911, 1907 and 1908.
  - **EBUSY.** Most failures were the known Windows `EBUSY` rmdir
    teardown flake, in the `cli-list-extraction-sources`,
    `cli-resolve-symbols`, `src/extraction/cli-runner` and
    `src/generation/cli-runner` test files.
  - **Load timing.** One loaded run also failed timing-sensitive tests
    in files Phase 1 does not touch: 2 stderr-capture tests in
    `lsp-client`, `typescript` `getDiagnostics`, and a 5 s timeout.
  - **Rerun alone.** Every failing file passes, including on the final
    tree:
    - `lsp-client`: 5/5;
    - `typescript`: 85/85;
    - `cli-list-extraction-sources`: 4/4;
    - `cli-resolve-symbols`: 10/10;
    - `src/extraction/cli-runner`: 33/33;
    - `src/generation/cli-runner`: 11/11.
  - **Targeted run.** The 13 touched or new test files pass on the
    final tree: 216/216.

**Real-data E2E** (the reproducer's zero-API recipe against the new
`dist/`; sentinel requests 0 and `api_calls=0` in all 8 runs):
- **Result.** Every F-4 and F-1 check passes. On b6729d3, every one
  except the runC1 staleness line failed (that one passed only because
  F-4 masked F-1).
- **runA** (three-stream v0.4 atlas, unchanged):
  - `files_deleted` 56 → 0;
  - docstring claims 377 kept, where b6729d3 left 0;
  - `source_shas` 80 kept, where b6729d3 left 24;
  - ADR links into docstring files 1423 kept, where b6729d3 left 0.
  - Symbols go 768 → 545, where b6729d3 left 219. 223 were pruned: 4
    no longer listed and 219 at excluded test paths. One claim was
    orphaned (`ts-adapter-parity-check`).
- **runB.** 6 of 6 commit claims and keys survive; b6729d3 kept 0.
- **runE.** Exit 0, and `validate-extraction` now "conforms (832
  claims across 86 sources)"; b6729d3 reported 449 across 24.
- **runC1.** Docstring claims 377 → 370: only the two deleted or
  renamed-away files' 7 claims go, and `docstring_sources_deleted=2`.
- **runC2.** Deleted and renamed-away symbols are pruned; the 5
  symbols of the new path are present.
- **runC3.** The `go.ts` (30) and `git-extractor.ts` (9) symbols are
  pruned. No dangling links remain (61 + 33 before), and 18 claims
  are orphaned.
- **No-op rerun.** A second pass on runA, runB, runE, runC1 and runC3
  exported nothing, and the atlas SHA-256 was unchanged.

**Open (not done in Phase 1):**
- F-5 normalization (Phase 2).
- F-7 `symbol_candidates` persistence (Phase 2/3).
- F-8 `resolve-symbols` exclude-pattern parity (decision needed before
  Tier 0 runs from hooks).
- **One-time prune on the dogfood atlas.** The first `index` after
  upgrading prunes the committed atlas's stale symbols: 229 of 1184.
  - 219 of them are at 41 excluded test/fixture paths, and 4 come from
    the deleted `cli-show-prompt.ts`. Both counts were checked
    statically.
  - The remaining 6 are symbols no longer listed, per the reproducer's
    inventory run.
- **Next paid re-index.** Appending the ADR-12 amendment changes that
  ADR's SHA, so the next `contextatlas index` re-extracts it; that
  costs money on the API path.

### Phase 2: three-stream CLI `index` (F-2)
- Absorb `scripts/dogfood-extract.mjs` behavior into the
  `AnthropicAPIDirectExtractor` path: ADR/docs → docstrings →
  commit messages, each SHA-gated. Streams are gated by
  `extraction.streams` (D-1 option (b); default: all three).
- PR follows CLAUDE.md §Pipeline Integration Discipline, with a
  stage-by-stage symmetry/divergence note against the ADR stream
  precedent.
- Retire `scripts/dogfood-extract.mjs` once parity is shown on the
  dogfood repo.

#### Phase 2 outcome (2026-09-25): committed in 854917d … f7bca66 (code), 29f9ca0 (S8 docs), a2caa41, c1c1003, 5261fbe and the round-2.3 commit (review fixes, code and docs); paid parity run pending

Built in eight commits, S1-S8, on top of 9e9310a, then four review-fix
commits (rounds 1, 2, 2.2 and 2.3). Lead decisions
L-1 to L-13 and L-15 (Travis, 2026-09-25) are recorded in §5. The
rules are recorded in the ADR-12 2026-09-25 amendment (stage table,
summary keys, exit semantics), the ADR-05 2026-09-25 amendment
(`extraction.streams`) and an ADR-02 revision entry.

| Step | Commit | What |
|---|---|---|
| S1 | `854917d` | Test hygiene (EBUSY cleanup retries; the `generate-adrs --yes` test made hermetic); `index --json` prints one JSON object on stdout; `--json` in `--help` |
| S2 | `a10d96a` | `extraction.streams` key, `resolveExtractionStreams`, doctor `config.extraction_streams` |
| S3 | `7ae8d3e` | Canonical commit key, `normalizeCommitKeys` (F-5), transactional commit writes, null-result pinning, `commit-log.ts` split, manifest `source_key` |
| S4 | `bddf294` | `symbol_candidates` persistence (F-7): cache migration 6, importer, exporter, CLI writers |
| S5 | `8fe4b14` | Retry-safe docstring core `extractDocstringFile` (L-10 i); legacy `extractDocstringsForFile` fixed through it |
| S6 | `9d2bf4c` | Three-stream `runExtractionPipeline`: Stage 0.5, plan, stderr cost preview, Stages 6c/6d, L-11 Stage 5 rule, appended summary keys, exit 1 on a failed stream |
| S7 | `f7bca66` | SKILL.md canonical keys and safe refresh rule; `list-extraction-sources` honours streams; `validate-atlas` bare-key warning; doctor `extraction.skills_fresh` |
| S8 | `29f9ca0` | ADR-05, ADR-12 and ADR-02 entries; DESIGN.md; README; CHANGELOG; this outcome; CLAUDE.md L-15 correction; `--full` help text |
| Review fixes | `a2caa41` | Fixes for the adversarially verified Phase 2 review findings; listed in the ADR-12 2026-09-25 amendment, "Review fixes (2026-09-25)" |
| Review fixes, round 2 | `c1c1003` | Fixes for the second review, of `a2caa41`; listed in the ADR-12 amendment, "Review fixes, round 2 (2026-09-25)" |
| Review fixes, round 2.2 | `5261fbe` | Fixes for the third review, of `c1c1003`; listed in the ADR-12 amendment, "Review fixes, round 2.2 (2026-09-25)" |
| Review fixes, round 2.3 | the "review round 2.3 fixes" commit | Fixes for the fourth review, of `5261fbe`; listed in the ADR-12 amendment, "Review fixes, round 2.3 (2026-09-25)" |

What shipped:
- **Streams.** `contextatlas index` extracts prose (`adr`), docstring
  and commit streams in that fixed order, one after another, as
  `extraction.streams` enables them (default: all three). They share
  one cost tracker and budget check. `runExtractionPipeline` without
  `deps.streams` stays prose-only (L-1 (b)), so the benchmarks driver
  keeps its stream set. Its atlases still change (`symbol_candidates`,
  the L-10 write rules of the library functions it calls); see the
  ADR-12 amendment's "Library callers" consequence.
- **Per-stream gates.** Docstring files are SHA-gated per file.
  Commits are key-gated, and `--full` does not force them (L-7). A
  source file that `docs.include` also matches is extracted as prose
  only.
- **Retry safety** (L-10). A docstring file is stored all-or-nothing.
  A commit write is one transaction; a null commit result is pinned
  with zero claims. A stream whose every call failed makes `index`
  exit 1 after the export. The commit stream is skipped without git.
- **Preview** (L-8 (a)). An estimate on stderr before the first call,
  only when a call is planned. The docstring call count is exact (a
  zero-API docstring read, cached for Stage 6c).
- **Summary** (L-9). Totals widened to all streams; nine keys
  appended after `unverified_symbol_files`.
- **New modules.** `pipeline.ts` went from 1081 lines (at 9e9310a) to
  473 at S8 (495 after round 1, 514 after round 2, 522 after round
  2.2, 529 after round 2.3) and is orchestration only. The stages
  live in `atlas-baseline.ts` (Stage 0, review fixes),
  `unsaved-work.ts` (resume carry-over, round 2), `extraction-plan.ts`,
  `cost-preview.ts`, `stream-stages.ts`, `run-cost.ts`,
  `prose-stream.ts`, `docstring-stream.ts`, `docstring-read.ts`,
  `commit-log.ts`, `pipeline-types.ts` and `atlas-export-stage.ts`; the
  review fixes added the cache-only storage modules `cache-meta.ts` and
  `source-key-streams.ts`, and round 2.2 added `foreign-commits.ts`,
  `source-key-files.ts`, `server-cache-load.ts` (MCP server startup
  load) and `atlas-symbol-reconcile.ts` (resolve-symbols links), and
  round 2.3 added `run-owner.ts` (which process set the unfinished-run
  mark) and `retry-keys.ts` (the cache-only retry list). madge over
  `dist/` finds no circular imports (S6, S7, review fixes).

**Gate evidence (per step, 2026-09-25; all without API calls or
spend):**
- **Tests.** Full vitest grew from 1912 tests (105 files, Phase 1) to
  2103 (116 files) at S7; S8 adds 1 (2104). The review fixes
  (`a2caa41`) reach 2151 (119 files; three full runs, all green); round
  2 reaches 2175 (120 files); round 2.2 (`5261fbe`) 2215 (125 files, as
  `vitest list` collects them there); round 2.3 2247 (128 files; three
  full runs on the final code, two green; the other failed three
  SKILL.md content tests only because the red-run stash pop, under
  `core.autocrlf=true`, had rewritten the working copy of SKILL.md with
  CRLF endings, which those regexes do not match; restored to LF, green).
  - Per step: S1 1915; S2 1959; S3 1982; S4 2000; S5 2024; S6 2070;
    S7 2103; S8 2104; review fixes 2151; round 2 2175; round 2.2 2215;
    round 2.3 2247.
  - The round-1, round-2 and round-2.3 commits wrote their regression
    tests first and ran them red against the previous code (stashed;
    in round 2.3, 23 regression tests), then ran typecheck, build and
    the full suite green. The round-2.2 commit does not record a red
    run.
  - Every step ran the full suite at least twice, and every recorded
    run on each step's final tree passed in full. S1's six final runs
    followed the switch to the async `rm` with retries.
  - Earlier in S1, EBUSY teardown failures showed up in 2 of 11 runs
    while cleanup used `rmSync` with retries.
- **Tests first.** Each step wrote its behaviour tests before the fix
  and recorded the red run in its report. The exceptions are S1's
  test-hygiene changes (EBUSY cleanup, which was measured, and the
  hermetic `--yes` test) and a few tests added alongside module
  splits, which pin moved code.
- **Typecheck and build.** `npm run typecheck` and `npm run build`
  exit 0 at every step. Test files are outside `tsconfig`; each step
  type-checked its changed test files with a scratch config.
- **Lint.** Not runnable: `npm run lint` finds no `eslint.config.*`
  (baseline, Arc 4 ESLint v9 row).
- **Zero-API probes against the built `dist/`**, with
  `ANTHROPIC_API_KEY` unset:
  - **This repository (S6),** with real tsserver and git, a temp copy
    of the committed atlas and a stub client. Run 1 planned 479 calls:
    13 prose files, 461 docstring calls across 108 files (122 files
    keyed) and 5 commits. The stub was called exactly 479 times, and
    the preview estimated $4.42 to $10.35. Run 2, at the same HEAD,
    made 0 calls, printed no preview and left the atlas
    byte-identical.
  - **F-5 on the three v0.8 Skill atlases (S6).** Bare commit keys and
    claim paths migrated: cobra 3/3, hono 70 keys and 71 claims, httpx
    3/3. A second pass changed nothing.
  - **F-7 (S4).** A copy of this repository's real local cache
    migrated from schema 5 to 6 with all 844 claims kept. For the
    Skill-atlas round trip, see F-7 in §2.
  - **Benchmarks compatibility.** The benchmarks repo's
    `scripts/v0.4-step5-mock-test.mjs` passed against the new `dist/`
    at S3-S6, and that repo's working tree was left unchanged.
  - **`index --json` without a key (S6).** Exit 2, empty stdout, no
    preview.
  - **Fresh-repo `init` (S7),** from the packed tarball without a key:
    skills, prompts and a scaffold with no `streams` key are written.
    `index` then fails at setup (exit 2) before any model call, and
    `init` exits 1. Nothing was spent.
- **Tarball (S7).** `npm pack --dry-run` lists 479 entries, including
  all three SKILL.md files and the new modules, with 0 test files.

**Pending: the paid parity run** (L-14, not yet approved). One paid CLI
`contextatlas index` on this repository, after all Phase 2 doc edits,
so each changed doc is re-extracted once. Suggested
`--budget-warn 15`. Parity means:
- per-stream call counts equal a zero-API `list-extraction-sources`
  enumeration of the same work (this assumes no `docs.include` glob
  matches a source file: the CLI extracts such a file as prose and the
  manifest lists its docstrings; this repository has no such overlap;
  ADR-02 2026-09-25 entry);
- claim shape and key format match the Skill conventions;
- a sanity ratio against v0.4 (374 claims from 151 calls);
- a no-op rerun makes 0 calls and leaves `atlas.json` byte-identical;
- a one-file touch re-extracts only that file's docstrings.

Byte or claim-count equality with the script is not the bar: model
output is nondeterministic, and the script has known differences
(TypeScript-only extensions, first-language inventory, the Q3 drop, no
SHA gate). The run also applies the one-time Phase 1 prune. After it:
remove `scripts/dogfood-extract.mjs`, rewrite the remediation in
`src/doctor/checks/atlas.ts` that names it, add the CHANGELOG
"Removed" entry, and recalibrate the preview's output-token priors
and the 3 characters-per-token estimate from the run's actuals.

**Open (not done in Phase 2):**
- **L-14 approval** for the paid parity run and its budget. The S6
  probe's plan was 479 calls, estimated at $4.42-$10.35. Re-sized at
  the S8 tree without API calls: 488 calls, estimated at $4.59 to
  $10.73.
  - 14 prose files (7 changed, 7 added);
  - 469 docstring calls across 109 files, from `list-extraction-sources`
    (the same filter as the CLI; assumes no docstring read fails);
  - 5 commits.

  The costs use the preview's own formula (`estimateStreamCost`).
- **Lead flag (S4): docstring candidates.** CLI docstring claims record
  only the model's candidates. The Skill puts the documented symbol's
  bare name first, which over-links colliding names once
  `resolve-symbols` re-resolves them. Confirm the CLI rule, or adopt
  the Skill's.
- **Lead flag (S6): L-11 gating.** A docstring key for a file that is
  no longer walked is deleted only while the docstring stream runs.
  Confirm this, or apply the rule whatever the enabled streams.
- **Skill parity gaps.**
  - The Skill never keys zero-docstring files, and it keeps a baseline
    key for an existing file whose docstrings are all gone.
  - *Closed by the review fixes:* a failed docstring read used to skip
    one symbol in the manifest but the whole file on the CLI; the
    manifest now leaves the whole file out too.
  - Skill `docs.include` extraction is deferred to Phase 6 (L-12 v).
- **Lead flags from the review fixes** (conservative choices made
  without a lead decision; each is easy to change):
  - `list-extraction-sources` emits `manifest_version: "2"` when a
    stream is disabled, though L-12 kept "1" for the additive
    `source_key`. Otherwise a pre-v1.2 installed `/index-atlas` reads
    the disabled streams' empty arrays as deleted sources and drops
    their claims. Alternative: always "2", which also stops old copies
    that mishandle canonical commit keys and docs-bucket prose.
  - An unfinished `index` run resumes when `atlas.json` is
    byte-identical to what it started from (a cache-only `_meta`
    record). Since round 2 the resume is additive: `atlas.json` is
    imported as on every run (ADR-06), and only the units the
    unfinished run stored are carried over it, a commit only when HEAD
    reaches it.
  - With `atlas.committed: false`, `atlas.json` only seeds an empty
    cache and is otherwise ignored with a warning. Since round 2 the
    MCP server and the `init` smoke test use the same rule (no
    symbols, claims or source keys); before, they imported into any
    cache without symbols.
  - Unparseable model responses (null result, malformed JSON) never
    count toward the L-10 (ii) stream failure; malformed JSON is
    treated as the L-10 (iii) null case for commits.
  - Cache migration 7 (`source_key_streams`) records which of the
    prose and docstring streams keyed each path, to resolve
    zero-claim keys. Cache-only; a fresh clone falls back to the old
    classification. Since round 2 the records are dropped whenever
    Stage 0 imports an `atlas.json` other than the one this cache last
    imported or wrote, which also falls back.
  - `validate-extraction` exempts from `adr_depth_floor` and
    `source_coverage` every path the prose walk does not put in the
    ADR bucket, except a missing file whose path names an ADR under
    `adrs.path` (round 2: the docs bucket comes from the walk, and a
    deleted docs page is exempt). `adr_claims_present` is unchanged.
  - `validate-atlas` reports claim links to unlisted symbols. Round 1
    made it an error; round 2 made it a warning (exit 0), because the
    repair is `resolve-symbols`, the next Skill gate, and the error's
    other remedy lost links.
- **Lead flags from review round 2** (same basis):
  - `/index-atlas` may write `symbols: []` on a refresh when the
    baseline `symbols` array is too large to re-write, keeping
    preserved `symbol_ids`; the atlas is not loadable until
    `resolve-symbols` runs. "Too large" is left to the agent. A
    zero-API alternative is for `resolve-symbols` to take the baseline
    symbols from git. (Round 2.2 does that for the gap only: the
    records of claim links `symbols` does not list come from the
    `atlas.json` committed at HEAD, and the Skill now runs
    `resolve-symbols` before `validate-extraction` in that case.)
  - A Skill refresh keeps a deleted docs-bucket page's key and claims
    (L-12 ii); the CLI deletes them. Round 2 only unblocked the
    validator gate. Parity would need the Skill to drop the key when
    the file is gone, as it does for ADR keys.
  - A prose `ParseError` no longer counts toward the prose all-failed
    throw (the L-10 ii rule of the other streams). A model change that
    makes every prose response unparseable now shows as
    `extraction_errors` with exit 0, not as a thrown error.
  - With `atlas.committed: true` and no `atlas.json`, every `index`
    run writes it, even a run that changes nothing.
- **Lead flags from review round 2.2** (same basis):
  - `resolve-symbols` reads the `atlas.json` committed at HEAD
    (`git show`) for the records of claim links `symbols` does not
    list, and exits 1 without writing when a link into a file it could
    not list (listing failed, language not configured) still has no
    record. It used to drop such links (exit 0). Fail-loud was chosen
    because the dropped links could not be recovered.
  - With `atlas.committed: true` the MCP server re-imports
    `atlas.json` at startup whenever it differs from the file the cache
    last imported or wrote, except during an unfinished `index` run.
    Phase 4 hot reload builds on this; the startup rule already
    changes when a teammate's cache is replaced.
  - Local cache migration 8 (`source_key_streams_run_start`, cache
    only) saves the key-stream records at each run's start so a resume
    can roll them back.
  - `list-extraction-sources` does not apply the CLI's `docs.include`
    collision skip (a source file a docs glob also matches). Applying
    it would give L-14 parity on such repos but leave the file
    unextracted on the Skill path, which has no `docs.include` prose
    until Phase 6. Left as is and documented (ADR-02).
  - `validate-extraction` treats a deleted path as a docs page when,
    read relative to the config root, it lies outside `source.root`
    and matches a `docs.include` glob. In the ADR-08 layout with a
    root-level glob such as `*.md`, a deleted ADR stored as
    `ADR-02-x.md` therefore reads as a docs page and is not checked.
  - The Skill's refresh rule 4 now describes how ADR keys are stored,
    but applying it is still up to the agent; no gate fails on a kept
    key whose ADR file is gone (the CLI never leaves one).
  - *Changed by round 2.3:* the server's hold during an unfinished run
    now applies only while that run's process is alive, and the
    `validate-extraction` config-root reading only in the ADR-08
    layout; see the next list.
- **Lead flags from review round 2.3** (same basis):
  - The MCP server tells a running `index` from a dead one by the pid
    and host Stage 0 records with the mark (`process.kill(pid, 0)`). A
    mark from another host (a cache on a shared drive) or with no
    owner record (a cache from before round 2.3) counts as running,
    and a recycled pid makes a dead run look alive; the warning then
    names the way out (delete the local cache). For a dead run whose
    `atlas.json` has changed, the server re-imports and drops the mark
    with the run's unsaved work. `index` would not have carried that
    work over the changed file either, but it could have after a
    branch switch back to the file the run started from; that resume
    is now lost once the server starts in between. Alternatives: a
    heartbeat with a staleness bound, or git's 12-hour rule.
  - With `atlas.committed: false` the MCP server still never loads a
    changed `atlas.json` over a non-empty cache (the round-1 rule); it
    now only warns, and the Skill tells the user to delete the local
    cache. Since round 2.3 every `index` run that changes the cache
    without importing `atlas.json` clears `index.key_streams_atlas_sha256`,
    so a present value that differs from `atlas.json` means no such run
    has changed the cache since it imported or wrote an earlier
    `atlas.json` (short of an interrupted committed run before the
    switch). Loading `atlas.json` in that case (in the server, and for
    symmetry in Stage 0) would deliver Skill refreshes in that mode
    without losing local-mode work. Not done: it changes the
    `committed: false` rule.
  - `index` now runs `validate-extraction` on every
    `atlas.committed: true` run, not only on exporting runs, so a run
    that extracts nothing exits 1 over a committed atlas that fails the
    depth floor (a teammate's, or an older one), where it exited 0.
    `init` still skips extraction, and validation, when `atlas.json`
    matches HEAD.
  - Two new cache-only `_meta` keys: `index.unfinished_run_owner`
    (`{pid, host}`) and `index.retry_source_keys` (the retry list). A
    prose or docstring file that fails on every call is re-billed on
    every run until it succeeds, as a changed file's failure already
    was.
- **Stale setup error.** The `index` missing-key error still suggests
  `architecture: claude-code-only`, deprecated since v0.7.
- **Loose ends.** `.contextatlas/extraction-sources.json` is not
  gitignored by `init`. `list-extraction-sources` passes
  `lsp.initialize_timeout_ms` to its adapters since round 2.3; the MCP
  server and doctor's LSP checks still do not. Optional C4 (key
  normalization in `resolve-symbols`) was not done; the next CLI
  `index` migrates instead.
- **F-8** (Phase 3) is unchanged.
- **Benchmarks.** Atlases regenerated with the new `dist/` will carry
  `symbol_candidates` on CLI claims. That is an atlas-substrate change
  to tag and control for (F1/F9). The benchmarks driver still runs its
  own docstring and commit passes; moving it onto the pipeline streams
  is a separate, deliberate change.

### Phase 3: Tier 0 structural refresh + pending queue
- `contextatlas index --structural`: Stages 0–5 + git signal +
  Phase 1 pruning; **skips all model calls**.
  - Changed/added sources across all three streams go into a
    **pending queue** (local cache table; deduplicated by source key,
    so N edits = 1 pending entry) instead of being extracted.
  - Test seam: injected extraction client that throws, which proves
    zero model calls.
- Exports `atlas.json` when structure changed (symbol lines, pruned
  symbols, git signal). Claims are untouched except orphan reporting.
- Pending queue + run log live in gitignored `.contextatlas/local/`.
  This establishes the local-only namespace the v1.3 session overlay
  will reuse.

### Phase 4: MCP server hot reload
- Watch `atlas.json` (`fs.watch` + mtime/content-hash guard,
  debounced). Re-import inside a transaction and swap. In-flight
  queries complete against prior state. (Since the Phase 2 review,
  round 2.2, the server already re-imports a changed `atlas.json` at
  startup with `atlas.committed: true`, by the file's SHA-256 against
  the one the cache last held; `server-cache-load.ts`.)
- Log reload events. Survive malformed or partially-written atlases:
  keep the previous state, warn, and retry on the next change.
- **Server wiring (deferred from Phase 0).** Decide whether to replace
  the hand-wired `server.connect(new StdioServerTransport())` in
  `src/index.ts` with SDK v2's `serveStdio(factory)`
  (`@modelcontextprotocol/server/stdio`). `serveStdio` builds a server
  instance per connection from a factory and serves both the 2025-era
  `initialize` handshake and the 2026-07-28 era (`server/discover`).
  Today `server/discover` returns -32601, and a client pinned to
  2026-07-28 cannot negotiate. The factory shape has to fit the
  hot-reload state swap above.

### Phase 5: git hooks + trigger policy
- `contextatlas hooks install | uninstall | status`.
  - Tier 0 runs detached and non-blocking on post-commit / post-merge
    / post-checkout.
  - Tier 1 is queued on `pre-push` (non-blocking; the push is never
    delayed).
- Hooks **chain** with existing hook content (marked block,
  idempotent install, clean uninstall). They respect
  `core.hooksPath`. Shell + `.cmd` shims, no husky (dependency
  minimization).
- **Nightly schedule available, not default:**
  `contextatlas refresh-claims --if-due` is safe for any scheduler.
  Docs ship cron + Windows Task Scheduler recipes. No resident
  daemon.
- `contextatlas init` offers hook installation (opt-in prompt;
  `--yes` accepts).

### Phase 6: Tier 1 incremental claim refresh (subscription)
- `/refresh-atlas` skill, derived from `/index-atlas`. It processes
  **only pending sources**. It reuses the existing Skill-path gates:
  `list-extraction-sources --pending`, `validate-extraction`,
  `resolve-symbols`, `validate-atlas`. It loads the extraction prompt
  via the Read-tool artifact (frozen-prompt load-mechanism rule per
  CLAUDE.md).
- `contextatlas refresh-claims` runner spawns
  `claude -p "/refresh-atlas" --max-turns N` in the repo.
  - Lock file prevents concurrent runs.
  - Drains the queue on success; the overflow beyond caps stays
    pending.
- **Cost controls (hard requirement):**
  - batch, don't run per commit
  - per-run caps (`max_sources_per_run`, `max_turns`)
  - noise filter: vendored/generated/test paths; whitespace-only or
    docstring-unchanged source diffs don't enqueue docstring
    re-extraction
  - usage ledger (`.contextatlas/local/refresh-log.jsonl`)
  - `refresh.auto_claims: off` kill switch (Tier 0 keeps running)

### Phase 7: model de-pinning + cheaper-model calibration for Tier 1
- **De-pin models (Travis, 2026-09-24).** `EXTRACTION_MODEL` /
  `GENERATION_MODEL` are hard-coded to `claude-opus-4-7`, and ADR-02
  freezes the extraction model. Replace the single pin with
  config-selected models per role/tier. The existing unused
  `config.index.model` is the hook. Models are validated against a
  **calibrated allowlist** (a model enters the allowlist only with
  Step-9-style evidence). The Skill frontmatter pins follow the same
  allowlist.
  - ADR-02 amendment reframes the invariant from "frozen model" to
    "frozen prompt + calibrated model set".
  - Defaults stay on the currently calibrated model until evidence
    says otherwise.
  - Motivation: model generations turn over faster than cycles
    (4.7 → 4.8 → 5 → 5.5 all shipped since the pin), and Tier 1 needs
    a cheaper option.
- Step-9-style calibration against the frozen `EXTRACTION_PROMPT`
  (prompt text unchanged; only the model varies). Candidates:
  current Sonnet and Haiku (and newer Opus generations), against a
  `claude-opus-4-7` baseline.
  - Sample: incremental-sized diffs across TS / Python / Go / C#
    sources.
  - Pass criteria parallel to v0.3 Step 9: JSON parse, severity
    discipline, claim agreement vs baseline.
- Outcome options:
  - (a) keep Opus;
  - (b) an ADR-02 amendment allowing a calibrated cheaper model
    **for Tier 1 only** (Tier 2 stays frozen). The skill frontmatter
    model pin changes accordingly.
- Evidence archives at `docs/adr/incremental-model-calibration/`.

### Phase 8: doctor + staleness
- Extend `checkStaleness()` / doctor to report:
  - commits behind HEAD
  - pending-source count by stream
  - orphaned claims
  - hook install state
  - last Tier 0 / Tier 1 run time
  - Tier 1 ledger totals

### Phase 9: integration sweep + close
- v1.1 hotfix-triplet sweep:
  - (a) fresh-repo `init` with hooks succeeds end to end;
  - (b) `npm pack --dry-run` ships `/refresh-atlas` skill + hook
    shims;
  - (c) regex/allowlist surfaces accept the new source keys and the
    config section.
- Pattern 5 first-execution verification on the contextatlas repo
  itself (17-commit-stale atlas at entry) and on the apis repo.
- ADR finalization, v1.2.0 tag.

### ADR + config substrate
- **ADR-24 (new): Tiered automatic refresh.** Tiers, trigger policy,
  subscription-for-incremental decision, cost controls, hook
  contract, local-only namespace.
- **ADR-12 amendment:** `index --structural`, `refresh-claims`,
  `hooks` subcommands.
- **ADR-06 amendment:** `atlas.json` may be regenerated by hooks;
  `.contextatlas/local/` is never committed.
- **ADR-05 amendment:** additive `refresh:` section: `auto_structural`,
  `auto_claims`, `trigger` (`push` default | `threshold` | `nightly` |
  `manual`), `max_sources_per_run`, `max_turns`.
- **ADR-02 amendment (conditional on Phase 7 outcome (b)).**

---

## §4 Scope — out

- Federation Shape 1 + local session overlay (v1.3; shared overlay
  retrieval layer).
- Session → committed-claim graduation (v1.4+; new claim source per
  Arc 3).
- File-save watcher / resident daemon (rejected: cost amplification;
  commit and push boundaries are the meaningful change units).
- Workspace UI, session-memory indexer, CCR integration (new repos).
- New language adapters; C# resolver name-prefix investigation
  (deprioritized off the workspace critical path).

---

## §5 Decision points (surface at phase entry)

- **D-1 (resolved, Travis 2026-09-24): option (b).** Add an
  `extraction.streams:` config gate (ADR-05 additive amendment) that
  defaults to all three streams (adr, docstring, commit). The CLI cost
  preview reflects the enabled streams.
- **D-2 (Phase 6 entry):** confirm headless `claude -p` skill
  invocation behavior: non-interactive tool permissions, which
  account a background run bills to, and failure modes when usage
  limits are hit. Spike before building the runner.
- **D-3 (Phase 7 exit):** model choice for Tier 1 per calibration
  evidence.

**Phase 2 lead decisions (Travis, 2026-09-25).** All of the
implementation brief's recommendations were accepted:
- **L-1 (b).** `deps.streams` is optional in `runExtractionPipeline`;
  omitted means prose only (library behaviour). The CLI extractor
  passes `resolveExtractionStreams(config)`. The benchmarks repo is
  unchanged.
- **L-2.** Canonical commit key `commit:<sha>` for the `source_shas`
  key and `claims.source_path`. `normalizeCommitKeys` runs on every
  CLI run after the atlas import and before the baseline is read.
  Readers accept both forms permanently. When a sha exists in both
  forms the prefixed claims are kept and the bare duplicates deleted,
  with the count logged. The Skill writes the canonical form and reads
  both.
- **L-3.** `streams: []` and any list without `adr` are rejected with
  an actionable config error ("remove the key, or include adr"), as
  are unknown values, duplicates and non-strings. The list is stored
  in canonical order.
- **L-4.** The v0.4 Q3 ≥30 commit-claim floor is dropped; DESIGN.md
  records it as historical.
- **L-5.** F-7 is in Phase 2. Storage migration, importer, exporter
  (after `symbol_ids`, only when non-empty), an export-only reader,
  never on `Claim` or MCP output. CLI extraction writes candidates for
  all three streams. No atlas schema bump.
- **L-6.** Fixed order prose → docstring → commit, sequential; the
  config's list order is ignored.
- **L-7.** `--full` re-extracts prose and docstrings; commits stay
  key-gated.
- **L-8 (a).** The cost preview is an informational stderr block,
  printed only when model calls are planned. It names disabled
  streams, has no prompt and no "~3x lower" wording. Docstring calls
  are counted exactly by a zero-API read cached for Stage 6c;
  output-token priors are fixed per-stream low/high constants. No
  `--dry-run` and no `--streams` flag this phase.
- **L-9 (a).** Totals widened to all streams; `files_*` stay prose
  only; per-stream keys appended after `unverified_symbol_files`; no
  renames or reordering; `extraction_errors` entries stay
  `{sourcePath, error}`.
- **L-10.** (i) Docstring files are replaced and keyed only when every
  call succeeded, also in the legacy `extractDocstringsForFile`.
  (ii) A docstring or commit stream whose every call failed finishes
  the run and export, then `index` exits 1. (iii) A null commit result
  is pinned with zero claims and a warning. (iv) Without git the
  commit stream is skipped with an info log; other git errors warn;
  the run never fails for them.
- **L-11.** Disabled streams' claims are retained, frozen, with a
  doctor warning. A docstring key whose file exists but is no longer
  walked is deleted when a configured adapter owns the extension, and
  kept when the language is not configured.
- **L-12.** SKILL.md writes the canonical commit key and reads both.
  Its refresh rule never drops commit keys, docs-bucket prose keys or
  zero-docstring keys. `list-extraction-sources` honours
  `extraction.streams` and adds `CommitSource.source_key`
  (`manifest_version` stays "1"). Doctor gains a skill-freshness
  check. Skill docs-bucket extraction is deferred to Phase 6.
- **L-13 (a).** `init`'s first `index` runs all three streams, with
  the preview. The `init` scaffold writes no `streams` key.
- **L-14 (open).** The paid dogfood parity run and its budget await
  approval (Phase 2 outcome, "Pending").
- **L-15.** CLAUDE.md "Extraction cost framing" gets a dated
  correction note: the ~3x ratio matches the stale $15/$75 constants
  vs the true $5/$25, and extraction requests send no
  `cache_control`.
- **Side fixes accepted:** EBUSY retry options in subprocess-test
  cleanup; a hermetic `generate-adrs --yes` test; `index --json`
  prints exactly one JSON object on stdout; stale comments.

---

## §6 Verification (ship gate)

- Full `npm test` + typecheck + lint per phase commit.
- Tier 0: zero model calls (throwing-client seam). Symbol lines
  updated. Deleted/renamed source symbols pruned. Pending queue
  correct and deduplicated (20 commits over 3 files → 3 pending
  entries).
- Hot reload: running server reflects a structural refresh without
  restart. Malformed atlas write does not crash the server.
- Hooks: install alongside a pre-existing `post-commit` hook → both
  run and the commit isn't blocked. Uninstall restores the original.
  `core.hooksPath` honored.
- Tier 1: fixture with 2 pending → queue drains and `validate-atlas`
  passes. 10 pending with cap 5 → 5 remain. Ledger records each run.
- Pattern 5: contextatlas-on-itself and apis repo end to end.

---

## §7 Substrate references

- Plan of record: `~/.claude/plans/purrfect-jumping-koala.md`
  (2026-09-24 pivot assessment + re-sequenced roadmap).
- [`v1.1-2.0-ROADMAP.md`](../../../v1.1-2.0-ROADMAP.md): Arc 4 items
  absorbed (SDK upgrades; doctor staleness); Arc 1 shifted one slot.
- ADR-02 (index-time-only model calls), ADR-06 (committed atlas),
  ADR-11 (git signal; staleness `--check`), ADR-12 (CLI surface).
