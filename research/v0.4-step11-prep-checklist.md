# v0.4 Step 11 ship-gate prep checklist

**Status:** Authored during v0.4 Step 10 (synthesis). Locks
the pre-flight + execution + post-ship items for Step 11
(v0.4 ship gate).

**Last updated:** 2026-04-29.

---

## Pre-flight (must complete before ship commit)

### Version + documentation refresh

- [ ] **`package.json` version bump** `0.3.0` → `0.4.0` (main repo).
- [ ] **`CLAUDE.md` current-version section refresh.** Update
  "Current: v0.3 shipped 2026-04-28" → "Current: v0.4 shipped
  YYYY-MM-DD". Refresh strategic-arc + v0.4 outcome
  paragraph + v0.5+ candidates list pointer (now points to
  `research/v0.5-candidates.md` as canonical reference).
- [ ] **`ROADMAP.md` cycle-close.** Update v0.4 section status
  from `[IN PROGRESS]` → `[SHIPPED]`; v0.4 outcome paragraph
  added; v0.5+ candidate seeding cross-reference; revision-
  history entry for ship date.
- [ ] **`STEP-PLAN-V0.4.md` Step 16-style progress log close.**
  Final stamp with cycle-close summary mirroring v0.3 Step 16
  pattern. **Absorbs §10.1 process notes** (parser.test.ts
  fragility recurrence; pipeline-integration discipline; v0.X-
  tagged script modification discipline; trace-analysis set-
  difference framing; cost-projection-vs-platform-billing
  systematic discrepancy).
- [ ] **`v0.4-SCOPE.md` cycle-close stamp.** Success-criteria
  checklist marked complete; ship-doc reference; final
  cumulative cost line.

### Self-use atlas refresh

- [ ] **Decision required at Step 11:** re-run `dogfood-extract.mjs`
  at ship SHA (cost ~$2-4 script / ~$0.50-1 platform) for
  clean provenance OR explicitly document stale-atlas state
  at v0.4 ship.

  **Recommendation:** refresh — clean provenance worth ~$1
  platform cost. Self-use atlas should match v0.4 ship SHA,
  not point at pre-ship SHA `32deffe`.

  If refreshed: verify `.contextatlas/atlas.json` `generator.
  contextatlas_commit_sha` matches the ship-candidate SHA.

### Verification

- [ ] **Full test suite green at ship-candidate SHA.** `npm
  test` (not just typecheck + lint per CLAUDE.md "Run npm
  test before committing src/ changes" discipline). All
  vitest suites pass; doctor smoke-tests; adapter
  conformance.
- [ ] **Doctor self-check green at ship-candidate SHA.** Run
  `node dist/cli.js doctor` against contextatlas itself; no
  FAILs; SHA-drift WARNs acceptable post-refresh-decision.

### Cumulative cost documentation

- [ ] **Final cumulative:** `$36.24 script + ($X dogfood-
  refresh if executed at Step 11) = $X final / ~$13-14
  platform actual`. Both well below $50 ceiling.

  Step 11 absorbs final cost numbers in cycle-close progress
  log.

  Per-step breakdown for reference (script-projected):
  - Step 5 atlas extraction (cobra + httpx + hono):
    `$24.43`
  - Step 7 dogfood extraction: `$7.62`
  - Step 9 bounded-validity matrix run: `$2.07`
  - **Subtotal pre-Step-11:** `$34.12`
  - Step 6 disclaimer drafting + Step 8 doctor + Step 10
    synthesis: ~$0 (no API spend)
  - **Earlier-cycle subtotal includes ~$2.12 misc** (Step 1
    LSP probe + Step 4 attribution diagnostic + Step 5
    httpx 3-sample diagnostic). Total v0.4 cycle: `$36.24`
    script-projected.

### Tag preparation

- [ ] **`git tag v0.4.0`** ready, message structured per v0.3
  pattern (1-line summary + bulleted highlights + cumulative
  cost line + reference to v0.4-SCOPE.md).

---

## Commit + tag (ship execution)

- [ ] **Single ship commit** lands version bump + doc
  refreshes (mirrors v0.3 Step 16 pattern). Commit message
  includes:
  - 1-line summary: "v0.4 ship: production-installability
    foundation"
  - Stream A / B / C deliverable highlights
  - Cumulative cost (final number)
  - v0.5+ candidate forward-pointer
- [ ] **Tag `v0.4.0`** applied to ship commit.
- [ ] **Push commit + tag** to `origin main` and
  `origin v0.4.0`.
- [ ] **Capture ship-commit SHA + tag confirmation** in
  conversation for record.

---

## Post-ship

- [ ] **Travis-owned launch-doc personal notes update**
  (Step 10.2; outside repo per scope-doc Option B).
- [ ] **Verify benchmarks repo also has v0.4 close artifacts.**
  Already landed: Step 5 atlases (cobra/httpx/hono refresh
  commits), Step 9 supplement amendment + trial files
  (commit `2efc7ba`). No additional benchmarks-repo work
  needed at Step 11 unless atlas-refresh cascade requires it.
- [ ] **v0.5+ planning queues; no immediate work.** v0.5
  thesis selection at per-version scope-doc drafting time.
  Canonical candidate list: `research/v0.5-candidates.md`.

---

## Items explicitly NOT in Step 11

- Quality-axis blind-grading methodology (v0.5+; candidate 10).
- Full statistical bounded-validity n>2 (v0.5+; candidate 11).
- Onboarding pipeline ergonomics (v0.5+; candidate 13).
- Adapter regressions surfacing post-ship — handled as point-
  fix patches, not gated by v0.4 ship.

These are bounded by scope-doc Step 9.4 lock + v0.5+
backlog discipline; surfacing them at Step 11 would
re-open scope.
