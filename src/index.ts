/**
 * ContextAtlas MCP Server — entry point.
 *
 * This file is a placeholder. On first run, replace with the actual MCP
 * server bootstrap:
 *
 *   1. Parse CLI args (repo path, config file path)
 *   2. Load and validate .contextatlas.yml
 *   3. Initialize storage (SQLite)
 *   4. Initialize language adapters based on config
 *   5. Start MCP server via @modelcontextprotocol/sdk
 *   6. Register tool handlers (get_symbol_context, etc.)
 *
 * See DESIGN.md and CLAUDE.md for implementation guidance.
 */

export function main(): void {
  throw new Error(
    "ContextAtlas entry point not yet implemented. See CLAUDE.md for build order.",
  );
}
