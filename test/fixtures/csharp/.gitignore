# .NET build artifacts auto-generated by csharp-ls / dotnet workspace
# load against CsharpProbe.csproj. NuGet restore output (project.assets.json,
# csproj.nuget.* manifest files) + per-target Debug/Release intermediates
# (AssemblyInfo.cs, GlobalUsings.g.cs, *.cache). The project file itself
# (CsharpProbe.csproj) is committed; restored / generated state is not.
# Mirrors the Ruby fixture .gitignore pattern (Gemfile committed, lockfile
# + vendor/bundle/ not).
obj/
bin/
