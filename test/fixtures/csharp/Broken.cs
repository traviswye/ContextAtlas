namespace CsharpProbe;

// Deliberate parse error — drives diagnostic delivery channel probe.
// Spike check 2: push (textDocument/publishDiagnostics notification)
// vs pull (textDocument/diagnostic request) model for Roslyn LSP.
public class Broken
{
    public void DoSomething(string arg

}
