# v0.7-SCOPE.md — Launch-bearing cycle to v1.0

**Status:** Phase 3 scope-doc drafted; cycle pre-planning Phase 1
+ Phase 2 + Phase 3 inheritance from v0.6 cycle close per Travis
pivot at v0.6 Step 7.5 (launch-bearing reframe locked at v0.6
Step 8.0 Q8.0.5 Refinement; substrate captured at
[`v0_7-HANDOFF.md`](v0_7-HANDOFF.md) launch-bearing reframe
section).

**Cycle character.** v0.7 is launch-bearing not substrate-
generation-bearing per Travis direction. Single ship-gate
integrated with v1.0 public launch trigger (Q-pre-5 lock; v0.7.0
+ v1.0.0 dual tags pointing at same commit).

---

## §1 Cycle thesis

V0.7 ships ContextAtlas v1.0 public launch via 3-tier scope:
PRIMARY claude-code-only extraction path (B13 functional
implementation closing single-dependency architectural
question); SECONDARY install + setup pipeline real-repo
empirical verification (closes V1.0 ship-gate criterion #2
via 3-repo verification target); TERTIARY backlog-drain
launch-blocking substrate gaps (3 items per Q-pre-3 lock).

Substrate-generation thesis preserved via post-launch
infrastructure inheritance (per Q9.0.11 lock at v0.6 Step
9.0): recruitment + observability + ADR-20 consent contract +
feedback template + onboarding documentation ship at v1.0
ready for v0.8+ post-launch real-world cohort exposure
execution. v0.7 cycle does NOT generate cohort substrate;
inherits v0.6-shipped infrastructure for v0.8+ consumption.

V1.0 ship-gate criteria post-v0.7 launch (honest 2-of-3 MET +
1 carried forward per launch-bearing reframe inheritance from
v0.6 Step 9.0 + Q9.0.10 lock):

- ✓ #1 quality-axis methodology parenthetical CLOSED at v0.5
  (preserved through v0.6 + v0.7)
- △ #1 statistically-meaningful-wins PARTIAL via v0.6 8-cell
  subset; CARRIED FORWARD to v0.8+ post-launch matrix-
  completion + F1-F9 methodology amendments
- ✓ #2 onboarding pipeline shipped — closes at v0.7
  SECONDARY 3-repo install/setup empirical verification
- ✗ #3 external dogfood trial NOT MET — Tier 3 cancellation
  carry-forward from v0.6 + v0.7 cycle; CARRIED FORWARD to
  v0.8+ post-launch cohort exposure execution against
  carried-forward recruitment infrastructure

**Cycle target:** launch-bearing timeline to v1.0 (internal
working target 4-5 days; public framing generic per (B) lock at
v0.6 Step 9.2.a A5 adjudication).

---

## §2 Cycle scope

### PRIMARY — claude-code-only extraction path (B13 functional implementation)

V0.6 shipped B13-flag stub per pending-resolution architecture
(Item 2 reconsider lock at v0.6 cycle-pre-planning; flags-with-
explicit-pending-resolution path b). v0.7 ships single-
dependency claude-code-only extraction path as functional
implementation.

**Scope:**
- B13 functional implementation: claude-code-only extraction
  pipeline (extraction-via-Claude-Code-session-context vs
  current Anthropic API direct dependency)
- ADR-02 amendment scope: extraction-sole-API-caller framing →
  extraction-via-multiple-paths (adjudicated at v0.7 Step 1
  design phase per Q-pre-1 lock; item-design-as-design-step
  convention)
- Cost model shift: API-pay-per-use → subscription-bounded for
  claude-code-only path
- Substrate inheritance: v0.6 flag-shape stub at A7 onboarding
  pipeline (Step 4.2 commit `f14cb04`; B13-flags `--cc-only`
  integration); v0.7 promotes to functional implementation

**Scope boundaries:**
- Default extraction path post-amendment locked at Step 1.0
  design phase per Q1.0.4. v0.7 ships claude-code-only
  extraction path as functional implementation; default-vs-
  opt-in configuration adjudicated at Step 1.0.
- Flag deprecation deferred to v0.8+ if approved per ADR-02
  amendment outcome
- Per-method cost projection re-modeling NOT in scope (v0.4
  cost-projection-vs-platform-billing 3x-reduction framing
  preserved; subscription-bounded cost model is binary
  enabled/disabled, not parameterized)

### SECONDARY — Install + setup pipeline real-repo empirical verification

V0.6 shipped pipeline-mechanics + smoke test (A4 + A6 + A7 + H5
+ B13-flags integration; Step 4.5 close commit `64819a7`).
v0.7 SECONDARY validates pipeline-mechanics against real-repo
empirical surface beyond Step 4.4 smoke test scope.

**3-repo target set per Q-pre-2 lock:**

1. **Travis personal projects** — real workflow context;
   primary substrate for "does it work on my actual code"
   verification
2. **ContextAtlas-on-itself dogfood** — operational baseline
   per CLAUDE.md dogfooding clause; control surface
3. **One external repo from cohort screening list** —
   external-developer-codebase verification surface;
   specifics firm at v0.7 Step 1 design phase per cohort
   screening list inheritance from v0.6-shipped recruitment
   infrastructure

**Fallback:** If 3-repo set surfaces too much variance for
launch-bearing timeline at Step 1 design phase, fall back to
2-repo set (drop external repo per Q-pre-2 fallback lock).

**Scope:**
- Run init + doctor + extraction + smoke-test pipeline against
  3 (or 2) target repos
- Capture friction observations (failure modes; UX gaps; user-
  trust-impacting bugs)
- Fix surfaced issues launch-blocking-tier OR document as
  v0.8+ candidates per honest-scope discipline #4
- v1.0 ship-gate criterion #2 closes when SECONDARY empirical
  verification passes (defined acceptance criteria firm at
  Step 1)

### TERTIARY — Backlog-drain launch-blocking substrate gaps (3 items)

Per Q-pre-3 lock + 3-source triage outcome:

1. **A1 `classifyError` catch-all conflates JSON-parse
   exceptions with API errors** (`research/v0.5-candidates.md`
   #1) — error reporting clarity at v1.0 launch; user-facing
   confusion when errors misclassified
2. **A2 `extractDocstringsForFile` non-idempotent at symbol
   level** (`research/v0.5-candidates.md` #2) — incremental
   reindex correctness on `contextatlas index` re-run; user
   trust on repeat invocation behavior
3. **A3 `pipeline.ts` Stage 5 deletion handling defeats
   Stream C idempotency** (`research/v0.5-candidates.md` #3)
   — paired with A2; deletion handling correctness for file-
   removed scenarios

**Scope:**
- A1 + A2 + A3 paired substrate-gap fixes
- ~90-300 LOC total scope estimate per v0.5-candidates.md
  framing
- Test coverage per CLAUDE.md src-changes-require-full-test
  discipline (1303/1303 baseline preserved at v0.7 ship gate)

---

## §3 Cycle non-scope

### Explicit DEFERRED to v0.8+ post-launch substrate cycle

Per launch-bearing reframe substrate (v0_7-HANDOFF.md launch-
bearing reframe section) + v0.6 Step 8.2 amendment + Q-pre-3
triage 3-source triage outcome:

**F1-F9 methodology amendments** (5 items per v0_7-HANDOFF.md
v0.6 cycle execution substrate section):
- Atlas-version-control methodology amendment (F1 + F9 tag-
  AND-control pattern)
- Cell-selection empirical pre-screen (F3)
- 2-axis retry policy (F4 + F6 reframing)
- Variance-control auto-stretch (F5)
- Causal mechanism investigation (F1 deferred work)

**Cohort exposure execution** — recruitment infrastructure
ships at v1.0 ready for v0.8+ post-launch real-world cohort
exposure cycle (Tier 3 pre-registration applied at v0.6 Step
7.5; recruitment + observability + ADR-20 contract + feedback
template + pre-trial onboarding all v1.0-ready substrate)

**Stream B matrix-completion** — full ~248-trial matrix
completion beyond v0.6 8-cell subset (statistically-meaningful-
wins gate work for V1.0 ship-gate criterion #1 closure)

**Stream A onboarding-completion remaining items** (not
launch-blocking per launch-bearing reframe narrowing):
- H1 hook/kickoff script for Claude Code priming
- H2 ADR generation pipeline (substrate-flexibility input
  dimension)
- H3 first-session-vs-returning-session routing logic
- C3 README/`docs/` parsing for architectural claims
- B12 session-state context engine general architectural
  pattern

**Stream D backlog-drain remaining items** (per Q-pre-3
triage; not TERTIARY-eligible at launch-bearing timeline):
- C1 task-shaped queries (`why_does_this_fail`,
  `onboard_to_feature`, `audit_change`)
- Slash commands (designed against v0.6 demand signal)
- A4 `buildBundle` requires adapter in atlas-only mode
  (`research/v0.5-candidates.md` #4) — atlas-only mode quirk;
  weaker user-trust impact than A1+A2+A3 paired gaps

**Phase-10 §9 cycle-emergent v0.7+ candidates** (5 items;
methodology improvements not launch-blocking):
- 529 retry-coverage observation (#1)
- h10 held_out filter discovery methodology refinement (#2)
- Variance trigger discipline (#4)
- Atlas-substrate-version-control methodology amendment (#5;
  composes with F1+F9)
- Cell-selection empirical-pre-screen methodology amendment
  (#6; composes with F3)

(F6 reframing per Phase-10 §9 #3 already shipped at v0.6
Step 5.3.a Path A swap-retry harness extension; not v0.7
scope.)

**v0.5-candidates.md residual unabsorbed v0.5+ items** (other
than A1+A2+A3+A4 already triaged):
- #5 Filter-shape vs content-richness three sub-candidates
  (feature gap)
- #11 Full statistical bounded-validity (n>2) — composes
  with Stream B matrix-completion deferral

### Items NOT new v0.7 scope (already substantively addressed)

- v0.5-candidates.md #6 Deep LSP health check — partially
  absorbed at v0.6 A6 doctor (Step 3.2.a + 3.2.b); v0.5-
  candidates.md needs absorption annotation refresh at v0.7
  Step 9.5 per Q9.0.6 α-light inheritance discipline (per
  observation captured at Q-pre-3 surface)
- v0.5-candidates.md #13 Self-use ContextAtlas-as-MCP
  onboarding pipeline — partially absorbed at v0.6 A7;
  SECONDARY at v0.7 closes empirical-verification dimension;
  v0.5-candidates.md needs absorption annotation refresh at
  v0.7 Step 9.5

---

## §4 Item-level descriptions

### PRIMARY — B13 functional implementation

**Inheritance from v0.6:** v0.6 shipped B13-flag stub at Step
4.2 (`f14cb04`; `--cc-only` flag wired into init/runner.ts +
config setup); flag exposes B13 architectural choice as user-
facing CONFIG-FLAG per Item 2 reconsider lock (path b — flags-
with-explicit-pending-resolution). v0.6 progress-log captured
architecture-assumption observations organically; v0.7 inherits
substrate.

**v0.7 work:**
- B13 architectural decision finalization at Step 1 design
  phase (ADR-02 amendment scope adjudication per Q-pre-1
  lock)
- claude-code-only extraction path functional implementation
  (~depends on Step 1 design phase outcome)
- Extraction pipeline path-routing logic (Anthropic API
  direct vs Claude Code session context branch)
- Cost model accounting for subscription-bounded path (zero
  per-call cost vs API pay-per-use)
- ADR-02 amendment text + commit (extraction-sole-API-caller
  → extraction-via-multiple-paths)
- Test coverage per CLAUDE.md src-changes-require-full-test
  discipline (mocked Claude Code session context responses;
  path-routing correctness; cost model accounting verification)

### SECONDARY — 3-repo install/setup empirical verification

**3-repo target set per Q-pre-2 lock:**
1. Travis personal projects (real workflow context; substrate
   firm at Step 1 design phase per Travis-side selection)
2. ContextAtlas-on-itself dogfood (operational baseline;
   control surface; existing substrate at
   `C:/CodeWork/contextatlas`)
3. One external repo from v0.6-shipped cohort screening list
   (specifics firm at Step 1 design phase; cohort screening
   list at `research/cohort/recruitment-infrastructure.md`
   per Step 6.4 commit `e9601e8`)

**Verification protocol per Step 1 design phase:**
- npm install (or equivalent install pathway pending B13
  outcome at PRIMARY)
- contextatlas init (per A7 onboarding pipeline; v0.6 Step
  4.4 + 4.5 substrate)
- contextatlas doctor (per A6 deep LSP health check; v0.6
  Step 3.2.a + 3.2.b substrate)
- Atlas extraction smoke test (per Step 4.4 commit `78f4a37`
  baseline)
- First-query smoke test per A7 success-message UX framing
  (Step 4.5 commit `9328bf0`)
- Friction observation capture (failure modes; UX gaps;
  user-trust-impacting bugs)

**Fallback:** 2-repo (drop external) if 3-repo variance
exceeds launch-bearing timeline at Step 1.

### TERTIARY — A1 + A2 + A3 paired substrate gaps

Detailed item descriptions inherited from
[`research/v0.5-candidates.md`](research/v0.5-candidates.md)
§Substrate gaps:

- **A1 (`research/v0.5-candidates.md` #1)** —
  `src/extraction/pipeline.ts` `classifyError` function
  conflates JSON-parse exceptions (model output malformed)
  with Anthropic API errors (network / rate-limit / 5xx).
  Fix: split classification; structured error payload
  surfaces specific failure mode to user.
- **A2 (`research/v0.5-candidates.md` #2)** —
  `src/extraction/pipeline.ts` `extractDocstringsForFile`
  function non-idempotent at symbol level: re-running on
  same file deletes + re-creates claim rows even when
  unchanged; affects incremental reindex correctness UX.
  Fix: symbol-id-keyed idempotency check before
  delete/re-create.
- **A3 (`research/v0.5-candidates.md` #3)** —
  `src/extraction/pipeline.ts` Stage 5 deletion handling
  paired with A2; current logic defeats Stream C idempotency
  on file-deletion path. Fix: claim-source-aware deletion
  sweep matching A2 idempotency model.

---

## §5 Substep ladder (firms at Step 1.0 per Q7 substep-ladder-firms-at-design convention)

**Step 0 (skipped at pre-planning).** No housekeeping items
identified at v0.7 cycle entry pre-planning. v0.6 cycle Step 0
captured B14 housekeeping (Opus 4.7 pricing fix; absorbed at
v0.6 Step 9.6 ship commit per scope-doc-locked-but-not-shipped
catch class observation). v0.7 inherits pricing fix at v0.6.0
baseline; no equivalent housekeeping item identified. If items
surface during v0.7 cycle execution that would retrospectively
fit Step 0 framing, surface as separate substep at appropriate
point in cycle (substep numbering preserves convention via
0.5-suffix or re-numbering).

Pre-planning estimate (firms at v0.7-SCOPE.md commit + Step
1.0 design adjudications):

- **Step 1** — PRIMARY B13 functional implementation
  - 1.0 design adjudications (Q1.0.1+; ADR-02 amendment scope;
    cost-model accounting; path-routing branch architecture)
  - 1.1+ implementation substeps (firm at 1.0 design phase)
  - 1.N close
- **Step 2** — SECONDARY 3-repo install/setup empirical
  verification
  - 2.0 design adjudications (Q2.0.1+; 3-repo target firm;
    verification protocol; acceptance criteria)
  - 2.1+ verification substeps
  - 2.N close
- **Step 3** — TERTIARY A1+A2+A3 substrate gap fixes
  - 3.0 design adjudications (Q3.0.1+; A1 split design; A2
    idempotency model; A3 deletion sweep design)
  - 3.1 A1 classifyError split implementation
  - 3.2 A2 extractDocstringsForFile idempotency implementation
  - 3.3 A3 pipeline.ts Stage 5 deletion handling implementation
  - 3.4 close
- **Step 4** — Cycle-close evaluations (cycle-close-bounded;
  matches v0.6 Step 8 pattern)
  - 4.0 design adjudications
  - 4.1 evaluations bundle
  - 4.2 v0_8-HANDOFF.md amendment + Step 4 close
- **Step 5** — Ship gate (v0.7 + v1.0 dual-tag per Q-pre-5
  lock; matches v0.6 Step 9 9-substep ladder pattern)
  - 5.0 design adjudications
  - 5.1 pre-flight verification
  - 5.2.a-d external-doc inline surfaces (README + ROADMAP +
    CLAUDE.md + DESIGN.md + LAUNCH.md if drafted)
  - 5.3 verification table inline
  - 5.4 tag message inline (v0.7.0 + v1.0.0 dual tag bodies;
    v0.7.0 cycle-close framing; v1.0.0 public-launch framing
    per Q-pre-5 lock)
  - 5.5 absorbed-item annotations (expanded scope per Q-pre-3
    observation: 5+ items including v0.5-candidates.md #6 +
    #13 + A1 + A2 + A3 absorbed-at-v0.7 annotations)
  - 5.6 ship commit + dual-tag HEREDOC alongside (single ship
    commit; v0.7.0 + v1.0.0 tags pointing at same commit per
    Q-pre-5 lock)
  - 5.7 cross-repo back-reference (benchmarks-repo Phase-N
    ref-doc revision history if applicable; potentially
    skip if no v0.7 cycle-execution Phase-N ref-doc was
    generated)
  - 5.8 SHA backfill

**Substep ladder shape note.** v0.7 has fewer steps than v0.6
(5 steps vs v0.6's 9 steps; PRIMARY + SECONDARY + TERTIARY +
cycle-close + ship-gate = 5 substantive work clusters).
Substep numbers firm at scope-doc commit + Step 1.0 design
adjudications.

---

## §6 Risks + mitigation

### Risk: Launch-bearing timeline pressure surfaces mid-cycle pivots

**Mitigation:** Substrate-evolution drift framework pre-
registered per Q-pre-4 lock. Path C (post-state framing)
default for substrate-evolution after substantive work shipped
against pre-state scope; Path A (update-pre-state framing) for
mid-cycle scope adjustment BEFORE substantive work shipped.
Surfaces handled honestly via amendment commits with explicit
scope-acknowledgment.

### Risk: B13 architectural decision adjudication blocks PRIMARY

**Mitigation:** B13 ADR-02 amendment scope deferred to Step 1
design phase per Q-pre-1 lock (item-design-as-design-step
convention). If Step 1 design surfaces architectural blocker
(e.g., Claude Code session context API unavailable for
production extraction), rescope condition #1 triggers (see
§10).

### Risk: 3-repo SECONDARY verification surfaces variance exceeding launch-bearing timeline

**Mitigation:** Q-pre-2 fallback lock — 2-repo (drop external)
fallback at Step 2.0 design phase. If 2-repo also exceeds
timeline, rescope condition #2 triggers.

### Risk: TERTIARY scope creep beyond 3-item lock

**Mitigation:** Q-pre-3 selection criteria explicit + 0-3 item
expected scope; 5+ surfaces scope creep risk. If A1+A2+A3 fix
scope substantively exceeds ~90-300 LOC envelope at Step 3.0
design phase, scope adjustment via Path A (update-pre-state
per Q-pre-4) — defer one or more items to v0.8+.

### Risk: V1.0 public launch trigger creates user-facing exposure before substrate ready

**Mitigation:** Q-pre-6 lock — launch-document publication
post-v1.0 ship; v1.0 ship triggers; documents publish on
Travis-readiness timeline (could be same day or +N days).
Window between v1.0 ship + launch-document publication is
feature: gives Travis time to verify v1.0 state cleanly before
public attention arrives.

### Risk: V0.6-cycle-introduced 7-class recursive catch-pattern observation classes recurring at v0.7 ship gate

**Mitigation:** Step 5.1 pre-flight working-content-gap-
inventory explicitly scans 7 catch-pattern classes per v0.6
Step 9 ship gate inheritance + adds NEW catch classes if
v0.7-specific surface (per recursive catch-pattern-coverage
discipline).

---

## §7 Ambiguity adjudications

### §7.1 Locked at pre-planning (Q-pre-1 through Q-pre-6 + entry option)

**Q-pre-entry — Pre-planning entry shape.** LOCKED γ hybrid
(light Phase-1 + Phase-3 direct). Skip Phase-1 broad-scan;
close-read merge against v0.7+ candidates from 3 sources
(v0_7-HANDOFF.md launch-bearing reframe + Phase-10 §9 +
v0.5-candidates.md residuals); Phase 3 v0.7-SCOPE.md drafting.

**Q-pre-1 — B13 architectural decision adjudication scope.**
LOCKED B13 functional implementation as v0.7 PRIMARY scope at
pre-planning. ADR-02 amendment scope adjudicated at v0.7 Step
1 design phase per item-design-as-design-step convention.

**Q-pre-2 — A7 install/setup empirical-test target-repo set.**
LOCKED 3-repo target set: Travis personal projects +
ContextAtlas-on-itself dogfood + 1 external repo from cohort
screening list. Fallback to 2-repo (drop external) if 3-repo
variance exceeds 4-5 day timeline at Step 2.0 design phase.

**Q-pre-3 — TERTIARY backlog-drain selection criteria + 3-
source triage.** LOCKED selection criteria (blocks v1.0 user
trust / narrative claims / ship-gate criterion #2 closure) +
3-source triage outcome: A1 + A2 + A3 substrate gaps absorbed
at v0.7 cycle TERTIARY scope (3 items per expected 0-3
envelope).

**Q-pre-4 — Substrate-evolution drift framework pre-
registration.** LOCKED Path C (post-state framing) default for
substrate-evolution after substantive work shipped against pre-
state scope; Path A (update-pre-state framing) for mid-cycle
scope adjustment BEFORE substantive work shipped against
superseded scope.

**Q-pre-5 — V1.0 ship-gate integration.** LOCKED v1.0 ship-
gate INTEGRATED INTO v0.7 ship gate; single ship-gate; no
separate v1.0 cycle. v0.7.0 + v1.0.0 dual tags pointing at
same commit (different tag bodies: v0.7.0 cycle-close framing;
v1.0.0 public-launch framing).

**Q-pre-6 — Launch-document publication coordination.** LOCKED
v1.0 ship triggers launch-document publication; launch-
document drafting does NOT block v1.0 ship. Window between
v1.0 ship + launch-document publication is feature.

### §7.2 Deferred to v0.7 Step 1.0 design phase

**Q1.0.1 — ADR-02 amendment scope detail.** Per Q-pre-1 lock;
firms at Step 1 design phase. Adjudication space includes:
permitted-modules expansion; extraction-via-multiple-paths
framing precision; backwards-compatibility for current
extraction-sole-API-caller users.

**Q1.0.2 — Claude Code session context API surface
verification.** Pre-implementation verification that Claude
Code session context API supports extraction-pipeline use
case. Could surface architectural blocker triggering rescope
condition #1.

**Q1.0.3 — Path-routing branch architecture.** Anthropic API
direct vs Claude Code session context branch — implementation
shape (config-flag-based dispatch vs runtime detection vs
hybrid). Firms at Step 1.

**Q1.0.4 — Default extraction path post-ADR-02 amendment.**
Adjudication space: (α) Anthropic API direct preserved as
default; claude-code-only opt-in via flag; (β) claude-code-
only becomes default; API direct opt-in via flag; (γ) API
direct deprecation path (defer to v0.8+ if approved). Affects
v1.0 user-facing default behavior + launch narrative framing.
Travis pivot framing ("claude code only is def a priority
feature") suggests (β) leans; (α) is defensive backward-
compatibility lock; (γ) too aggressive for v0.7 timeline.
Travis call at Step 1.0 design phase.

### §7.3 Deferred to v0.7 Step 2.0 design phase

**Q2.0.1 — 3-repo target firm.** Travis personal projects
specifics; external repo from cohort screening list specifics;
operational baseline scope.

**Q2.0.2 — Verification acceptance criteria.** What
constitutes SECONDARY closure? Friction-observation count?
Specific UX-pass criteria? Firms at Step 2.

**Q2.0.3 — Friction-observation handling protocol.** Surfaced
issues at SECONDARY: launch-blocking-fix-now vs document-as-
v0.8+-candidate vs honest-scope-acknowledgment. Triage
discipline at Step 2.

### §7.4 Deferred to v0.7 Step 3.0 design phase

**Q3.0.1 — A1 classifyError split design.** Specific
classification taxonomy (which error subclasses get which
user-facing messages); structured error payload shape.

**Q3.0.2 — A2 idempotency model.** Symbol-id-keyed
idempotency check shape; cross-references to A3 deletion
handling.

**Q3.0.3 — A3 deletion sweep design.** Claim-source-aware
deletion model; cross-references to A2 idempotency.

### §7.5 Deferred to v0.7 Step 4.0 cycle-close design phase

Inheritance from v0.6 Step 8.0 Q8.0.1-Q8.0.8 pattern;
adjudications firm at v0.7 cycle close per cycle-close
discipline.

### §7.6 Deferred to v0.7 Step 5.0 ship-gate design phase

Inheritance from v0.6 Step 9.0 Q9.0.1-Q9.0.12 pattern (12
sub-adjudications); adjudications firm at v0.7 ship-gate
design per Step N.0 cadence convention. Plus v0.7-specific:

**Q5.0.X — Dual-tag operation semantics.** v0.7.0 + v1.0.0
tag creation order (v0.7.0 first then v1.0.0; or both
alongside ship commit); tag body content scope per Q-pre-5
lock; SHA-free body inheritance from v0.5.0 + v0.6.0
precedent.

**Q5.0.Y — Step 5.5 absorbed-item annotation expanded scope.**
v0.5-candidates.md #6 + #13 partial-absorbed annotation
refresh at v0.7 (per observation surfaced at Q-pre-3
surface); A1 + A2 + A3 absorbed-at-v0.7 annotations; v0_7-
HANDOFF.md substrate annotations if applicable. 5+ items vs
v0.6 Step 9.5 4 items (B14+B16+C12+C14).

**Q5.0.Z — package.json version bump shape.** Single bump to
1.0.0 (public-launch version reflects canonical anchor) OR
two bumps (0.6.0 → 0.7.0 → 1.0.0; preserves cycle-internal
version step). Lean single bump to 1.0.0; dual-tag captures
cycle-internal (v0.7.0) vs public-launch (v1.0.0) framing
without dual version bumps. Adjudicated at Step 5.0 design
phase per Q-pre-5 dual-tag operation semantics inheritance.

---

## §8 Success criteria

V0.7 ships when:

1. **PRIMARY B13 functional implementation shipped + tested.**
   claude-code-only extraction path functional; ADR-02
   amendment committed; path-routing logic correct; cost
   model accounting verified; subscription-bounded path zero
   per-call cost. Test coverage per CLAUDE.md discipline.

2. **PRIMARY ADR-02 amendment shipped.** extraction-sole-API-
   caller framing → extraction-via-multiple-paths; ADR
   commitments documented; cross-references to v0.6 B13-flag
   substrate.

3. **SECONDARY 3-repo install/setup empirical verification
   passed (or 2-repo per fallback).** Verification protocol
   executed against locked target-repo set; friction
   observations captured; launch-blocking issues fixed at
   Step 2; non-launch-blocking documented as v0.8+ candidates.

4. **SECONDARY V1.0 ship-gate criterion #2 closure.**
   Onboarding pipeline empirical verification completes
   criterion #2 closure path established at v0.6 Step 4.5
   pipeline-mechanics ship.

5. **TERTIARY A1 classifyError shipped + tested.** Error
   classification split; user-facing error reporting
   improved; unit tests on classification taxonomy.

6. **TERTIARY A2 extractDocstringsForFile idempotency
   shipped + tested.** Symbol-id-keyed idempotency model;
   incremental reindex correctness on re-run; integration
   tests against fixture.

7. **TERTIARY A3 pipeline.ts Stage 5 deletion handling
   shipped + tested.** Claim-source-aware deletion sweep;
   file-deletion idempotency; integration tests against
   fixture.

8. **No production-tool claims published beyond v0.7 cycle
   execution supports.** Per discipline #4 honest-scope-
   acknowledgment + v0.5 + v0.6 honesty-discipline
   inheritance; v1.0 ship-gate criterion #1 statistical-
   wins gate remains open per matrix-completion deferral
   to v0.8+; criterion #3 external dogfood NOT MET per
   Tier 3 carry-forward.

9. **Backlog discipline preserved.** v0.8+ deferral-locks
   + dropped + pre-housekeeping all have explicit forward-
   pointers; no silent drops. v0_8-HANDOFF.md drafted at
   Step 4 cycle-close per v0_7-HANDOFF.md inheritance
   pattern.

10. **Substrate-evolution drift framework operational per
    Q-pre-4 lock.** Mid-cycle scope adjustments captured
    via Path A (pre-state) or Path C (post-state) per pre-
    registered framework; honest scope-acknowledgment
    discipline preserved.

11. **Test suites green pre-bump.** Main-repo `npm test`
    PASS (1303/1303 v0.6 baseline + v0.7 test additions
    for new B13 functional implementation modules + A1 + A2
    + A3 fix test coverage); benchmarks-repo green.

12. **Standard ship-gate criteria.** package.json bump per
    Q5.0.Z shape lock at Step 5.0 design phase (lean single
    bump to 1.0.0 reflecting public-launch canonical anchor);
    annotated tags `v0.7.0` + `v1.0.0` per v0.5 + v0.6 SHA-
    free precedent; STEP-PLAN-V0.7.md progress log complete
    with all step entries; 9-step locked sequence executed
    including Step 7.5 post-execution verification per v0.6
    + v0.5+ canonical inheritance; all commits pushed
    (subject to explicit Travis direction per CLAUDE.md no-
    push-without-explicit-direction discipline).

13. **V0.5-candidates.md absorption annotation refresh
    shipped at Step 5.5.** v0.5-candidates.md #6 (Deep LSP
    health check) + #13 (Self-use ContextAtlas-as-MCP
    onboarding pipeline) annotations updated to reflect
    v0.6 + v0.7 partial-absorption status; A1 + A2 + A3
    annotated as ABSORBED-at-v0.7 with ship-SHA forward-
    pointers per Q9.0.6 α-light inheritance.

14. **V1.0 launch-document drafting unblocked post-v1.0
    ship per Q-pre-6 lock.** Launch-document publication
    timing on Travis-readiness; v1.0 ship + push lands
    triggers launch-state-stable signal.

All fourteen criteria testable via committed artifacts. v0.7
launch-bearing target raises bar on accuracy of public claims
not on the criteria themselves; v1.0 launch trigger explicit
via Q-pre-5 dual-tag mechanism.

---

## §9 Cost framing

V0.7 envelope estimate per launch-bearing scope:

| Stream | Items | Cost driver | Estimate |
|---|---|---|---|
| PRIMARY B13 functional implementation | 1 (B13 + ADR-02) | Implementation work + testing; minimal API spend (claude-code-only path may use no Anthropic API at all once shipped; Anthropic API direct path preserved as default) | $$ |
| SECONDARY 3-repo install/setup verification | 1 (3-repo target) | Empirical verification API spend (extraction runs against 3 real repos; ~$5-15 per repo per cost-priors-v0.6.json substrate); friction-fix LOC scope variable | $$$ |
| TERTIARY A1+A2+A3 substrate gaps | 3 (paired fixes) | Implementation + testing; zero API spend (pure code + filesystem + tests) | $ |
| Cycle close + ship gate | 2 (Steps 4 + 5) | Documentation-bearing zero API spend per Q9.0.6 + Q8.0.1 inheritance | $0 |

**Cumulative v0.7 spend estimate:** ~$15-50 platform-billed
reconstructed (cycle execution cost driver SECONDARY 3-repo
verification; rough estimate against v0.5 ~$10.25 + v0.6 cost-
priors-v0.6.json substrate). Concrete reconstruction at Step 5.6
ship commit per cycle-close cumulative reconstruction; matches
v0.6 cumulative cycle spend reconstruction discipline (TBD-
with-precedent-anchor framing).

**Honest scope-acknowledgment.** Cost-projection-vs-platform-
billing 3x reduction systematic preserved per Q5 lock + v0.4
discipline; concrete v0.7 reconstruction at ship commit if
substantively different from precedent.

---

## §10 Rescope conditions

V0.7 rescopes (not kills — rescopes) if:

1. **B13 architectural blocker surfaces at Step 1 design
   phase.** Claude Code session context API unavailable for
   production extraction OR substantive architectural friction
   beyond Step 1 design envelope → PRIMARY scope adjustment
   via Path A (per Q-pre-4 lock); B13 functional implementation
   may defer to v0.8+ if blocker substantive; v1.0 launch
   timing affected; Travis call.

2. **SECONDARY 3-repo verification variance exceeds 2-repo
   fallback at Step 2.0 design phase.** 3-repo too variable
   AND 2-repo also exceeds timeline → SECONDARY scope
   adjustment; potentially descope to ContextAtlas-on-itself
   only OR defer SECONDARY closure criterion to v0.8+;
   honest-scope-acknowledgment per discipline #4.

3. **TERTIARY A1+A2+A3 fix scope exceeds ~90-300 LOC
   envelope at Step 3.0 design phase.** Substrate gap fixes
   substantively heavier than v0.5-candidates.md framing
   estimated → defer one or more items to v0.8+; scope
   adjustment via Path A.

4. **Test coverage regression at v0.7 ship gate.** 1303/1303
   v0.6 baseline + v0.7 additions does NOT pass at Step 5.1
   pre-flight → STOP; investigate root cause; do NOT bypass
   per CLAUDE.md src-changes-require-full-test discipline.

5. **Substrate-evolution drift exceeds Path C / Path A
   framework capacity.** Mid-cycle pivots substantively
   reframe v0.7 thesis → Travis call; potentially re-draft
   v0.7-SCOPE.md amendment OR pause v0.7 cycle for v0.7-
   SCOPE.md re-planning per discipline #4 honest-scope.

6. **Recursive catch-pattern at ship gate surfaces 7+ multi-
   cycle drift instances.** v0.7 Step 5.1 pre-flight working-
   content-gap-inventory finds substantively more drift than
   v0.6 Step 9.1 21-instance baseline → STOP; investigate
   discipline #3 cadence application gaps; do NOT bypass per
   recursive catch-pattern discipline.

7. **V1.0 public launch readiness gap surfaces post-Step 5
   ship gate.** Discovered between v1.0 ship + launch-
   document publication window → Travis call; potentially
   amend v1.0 state via Path X (per v0.5 ship gate Step 7.5
   pre-push-window-affordance precedent); launch-document
   publication delayed pending fix.

These aren't automatic kill-switches — they're triggers to
stop adding work and re-plan in the open per ROADMAP rescope
discipline.

---

## §Sequencing

Per launch-bearing reframe substrate inheritance:

**Cycle execution sequencing:**
1. v0.7-SCOPE.md commit (this commit); STEP-PLAN-V0.7.md
   initialization at Step 0 OR direct Step 1.0 entry per
   Q1.0 design phase
2. Step 1 PRIMARY B13 functional implementation
3. Step 2 SECONDARY 3-repo install/setup verification (in
   parallel with Step 1 OR sequential per Step 1 design
   phase outcome on path-routing implementation timing)
4. Step 3 TERTIARY A1+A2+A3 substrate gap fixes (in parallel
   with Step 2 OR sequential per resource-availability)
5. Step 4 cycle-close evaluations (matches v0.6 Step 8
   pattern)
6. Step 5 ship gate (matches v0.6 Step 9 9-substep ladder
   pattern with v0.7-specific dual-tag adjustments)

**Ship-gate inheritance notes (per Q-pre-3 surface
observation):**

V0.7 Step 5.5 absorbed-item annotations inherits **expanded
annotation scope** vs v0.6 Step 9.5 (5+ items vs v0.6 4 items
B14+B16+C12+C14):

- v0.5-candidates.md #6 Deep LSP health check (partially
  absorbed at v0.6 A6 doctor; commits `52ee846` + `2cef348`)
- v0.5-candidates.md #13 Self-use ContextAtlas-as-MCP
  onboarding pipeline (partially absorbed at v0.6 A7;
  commits `78f4a37` + `9328bf0`)
- A1 classifyError absorbed-at-v0.7 via TERTIARY scope
- A2 extractDocstringsForFile absorbed-at-v0.7 via TERTIARY
  scope
- A3 pipeline.ts Stage 5 deletion absorbed-at-v0.7 via
  TERTIARY scope
- Plus v0_7-HANDOFF.md §4 row absorbed-item annotations if
  applicable (B13 functional implementation absorption?
  ADR-02 amendment absorption? — firms at Step 5.5)

**Path 1 launch-document coordination preserved.** Travis
launch-document drafting (Reddit posts; blog post; README
header rewrite) post-v1.0 ship per Q-pre-6 lock. Step 5.2.a
README doc-refresh ships canonical v1.0 state; Travis launch-
side polish post-ship.

---

## §Revision history

- **2026-05-09 (Phase 3 v0.7-SCOPE.md initial drafting)** —
  v0.7 cycle pre-planning Phase 1 + Phase 2 + Phase 3
  inheritance from v0.6 cycle close; γ hybrid entry option +
  6 Q-pre adjudications locked; 3-source TERTIARY triage
  outcome 3-item lock (A1 + A2 + A3). Scope-doc structure
  mirrors v0.6-SCOPE.md with launch-bearing adjustments
  (narrower scope: 3 streams → 3-tier scope; 9 steps → 5
  steps; 15 success criteria → 14 success criteria; v1.0
  ship-gate integration via Q-pre-5 dual-tag). Substrate-
  evolution drift framework pre-registered per Q-pre-4 lock.
  3 refinements applied at adjudication: Q1.0.4 default-path
  adjudication added to §7.2 + §2 PRIMARY scope boundaries
  reframed to defer default-path lock; Step 0 explicit-skip
  note added to §5 substep ladder; Q5.0.Z package.json bump
  shape adjudication added to §7.6 + §8 criterion #12
  reframed to defer bump shape to Step 5.0 design phase.
  V0.7-SCOPE.md ships at this commit; STEP-PLAN-V0.7.md
  initialization at Step 0 OR Step 1.0 entry per next-
  session direction.

---

## §Cross-references

- [`v0_7-HANDOFF.md`](v0_7-HANDOFF.md) — canonical bridge
  document for v0.7 cycle pre-planning; launch-bearing
  reframe section + Phase 2 4-stream historical record +
  cycle-pre-planning insights + v0.6 cycle execution
  substrate F1-F9
- [`v0.6-SCOPE.md`](v0.6-SCOPE.md) — v0.6 scope anchor
  (shipped 2026-05-09; commit `a8d01eb`); 15-criterion
  pattern + 22-Q-list + cycle-pre-planning Phase 1+2
  narrative inheritance
- [`STEP-PLAN-V0.6.md`](STEP-PLAN-V0.6.md) — v0.6 cycle
  per-step execution log; ship gate 9-substep ladder pattern
  + 7-class recursive catch-pattern observation enumeration
  for v0.7+ inheritance
- [`research/v0.5-candidates.md`](research/v0.5-candidates.md)
  — substrate-gap items A1+A2+A3 source + #6+#13 partial-
  absorption annotation refresh targets at v0.7 Step 5.5
- [Phase-10 ref-doc](https://github.com/traviswye/ContextAtlas-benchmarks/blob/main/research/phase-10-v0.6-reference-run.md)
  §9 — cycle-emergent v0.7+ candidates inventory; §11 F1-F9
  findings + §12 revision history (cross-repo back-reference
  surface for v0.7 ship gate inheritance)
- [`docs/adr/ADR-02-extraction-sole-api-caller.md`](docs/adr/ADR-02-extraction-sole-api-caller.md)
  — ADR-02 amendment scope target at Step 1 design phase
  (extraction-sole-API-caller → extraction-via-multiple-
  paths)
- [`docs/adr/ADR-19-llm-judge-methodology.md`](docs/adr/ADR-19-llm-judge-methodology.md)
  — v0.5+ methodology cross-cutting ADR; v0.7 inheritance
  preserved (Sonnet 4.6 judge unchanged; Opus 4.7 matrix-run
  model unchanged)
- [`CLAUDE.md`](CLAUDE.md) — "Cost-priors interpretation
  discipline" v0.5 Step 10.1 lock + v0.6 first-cycle-
  consuming inheritance preserved at v0.7
- [`ROADMAP.md`](ROADMAP.md) v0.7 [PLANNED] section — launch-
  bearing reframe public-facing canonical surface
